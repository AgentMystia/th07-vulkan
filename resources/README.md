# Resources

This directory is reserved for redistributable placeholders and build-time metadata.

Original TH07 game files and extracted assets belong in `reference/`, which is ignored by Git. Runtime code should load the user's local original data from `reference/original/` during development, and from a user-provided install path once platform packaging is implemented.
