# TH07 Reconstruction Status

Current verdict: not playable-complete and not 100% synchronized yet.

This file tracks the gap between the current C++ reconstruction and the requested DONE condition: behavior matches TH07 static decompilation and original data.

The repository goal now includes the TH06-project-style operational bar: direct local compile, a directly launchable executable, original TH07 resource loading, and playable original-style game flow. Function-level runtime replacement state is tracked in `config/runtime_replacement_manifest.csv`; this file summarizes durable subsystem facts only.

`config/runtime_replacement_manifest.csv` is now the authority for live runtime status. Rows marked `legacy-th06`, `pending`, or `blocked` are not playable-complete even when nearby helper tables or parsers are source-derived.

## Current Checkpoint Labels

- Exact / build-validated: flat `src/` project wiring, local reference-path discovery, resource manifest lookup shape, SHT binary parser coverage, portable ANM/STD/MSG/ECL binary parser coverage, Stage STD loader contract PE/data test coverage, GUI MSG loader contract PE/data test coverage, ANM manager load/free contract PE/data test coverage, compiled resource-format metadata, ECL opcode-gap/direct-effect/call-contract metadata, source-derived RNG/math helper tests, TH07 timer tick/increment/decrement helper tests, TH07 clear-bonus helper/owner-field tests, TH07 Cherry/Border HUD anchor tests, TH07 GUI spell-card layout tests, and platform-independent Player layout constants covered by CMake tests.
- Source-derived / anchored: SHT path table/order, SHT header movement speeds, the 12-file SHT shot summary table, SHT callback addresses and high-level semantics, `FUN_004423e0` init-time SHT owner pointer loads into Player `+0xb7e70/+0xb7e74`, TH07 `FUN_00431330` FileSystem archive lookup contract, single archive owner `0x00626258`, `FUN_00438668`/`FUN_00438de2` `th07.dat` open-close lifecycle, and the compiled `Th07FileSystemRuntime` archive-open/non-external read helper, TH07 RNG seed transition/U32/float-scaling/angle-wrap helpers, TH07 `FUN_0043958d` frame/subframe timer tick helper, TH07 `FUN_00439401`/`FUN_004394c7` timer increment/decrement helpers including `DAT_00575adc` bit 5 force-step behavior, TH07 `FUN_00418fc0` force-step bit-5 producer contract, TH07 `FUN_0042d8d5` GUI score clamp/step and high-score update helpers, TH07 `FUN_0042d83a` life/bomb extend-award helper fields/flags, TH07 `FUN_00432990` point-item extend threshold formula/call sites, Player/Bomb routine tables, Player ANM VM prefix and collision field offsets, Player movement input/direction/focus-speed/clamp contract from `FUN_0043ee50`, Player raw mode/option/common-effect owner fields, Player active-effect pointer tail slots, Player mode active-effect lifecycle contracts, Player mode update/draw runtime branch contracts, bomb score-drain/speed/common-effect metadata, ANM per-file header metadata, ANM VM opcode usage and argument-size coverage, portable ANM binary parser coverage including all 63 extracted binaries and aggregate opcode/argument-shape validation, the 63-entry runtime ANM string manifest, the 46-entry runtime ANM load contract table, the `FUN_0044df90`/`FUN_0044e070`/`FUN_0044e4e0` ANM manager load/free slot contract, the TH07 enemy bullet ANM resource anchor plus 10 resolved bullet template script/graze rows, portable STD binary parser coverage including all eight stage binaries and Stage 6 binary-only validation, `FUN_00407610` Stage STD loader contract coverage including owner fields, object-table relocation, `0x24c` quad VM allocation, object active flag writes, and ANM script setup, portable MSG binary parser coverage including all eight message binaries and Stage 6 opcode `0x0e` validation, `FUN_00429935`/`FUN_004299f8` GUI MSG loader/free contract coverage including owner offsets, current instruction/index initialization, raw entry-pointer relocation, and cleanup, STD/MSG resource-format coverage including STD and MSG binary opcode usage and argument-size coverage, portable ECL binary parser coverage including all eight extracted binaries, 762 subs, 17 timelines, 19919 sub instructions, 2494 timeline instructions, and 141 unique sub opcodes, ECL manager load/call-sub contract from `FUN_0040e420`/`FUN_0040e530`, ECL declaration/dispatch gap coverage, the corrected `FUN_00410520` `opcode - 1` jump-table mapping at `0x00417838`, 41 direct high-opcode owner write contracts, 17 helper-call/resolved-target high-opcode call contracts, 70 `FUN_0040e5b0` int-read resolver contracts, 74 `FUN_0040edf0` float-read resolver contracts, 37 `FUN_0040f3c0` resolved float write-target contracts, the `FUN_00424740` ECL clear-helper surface, the `FUN_00424c00` ECL item-clear radius helper, the `FUN_004326f0` ECL visual-effect spawn contract, Effect table/layout metadata, Stage camera/ANM VM opcode slices, clear-bonus constants and `+0x209b4..+0x20a2c` owner fields, `FUN_00427c81` bonus-toast registration contract, `FUN_00427f22` bonus-toast kind rules, `FUN_0042adab` bonus-toast update/expiry contract, seven bonus-toast producer callsites, `FUN_0042868d` GUI spell-card display offsets, `FUN_00429c42` full 15-entry MSG dispatch table with common advance/post-tick paths, MSG opcode `0x01` portrait script base, opcode `0x02` portrait sprite base, opcode `0x0c` music-fadeout, opcode `0x0d` wait-skippable byte write, opcode `0x0e` screen-fade behavior, and Cherry/Border notification labels.
- Transitional: `src/Player.cpp` now initializes the source-derived low-byte Player mode/option/request state and `FUN_004423e0` common-effect defaults, and its live character-data table covers all six TH07 shot types with movement speeds sourced from `Th07PlayerShotTables`. Sakuya shot callbacks deliberately stop spawning until `.sht` runtime dispatch is wired. `src/BulletManager.cpp` now uses the source-derived single `data/etama.anm` load and the first 10 resolved `DAT_00495160` template rows, while broader spawn/update/collision and laser behavior are still TH06-shaped. `src/BombData.*` has six live table slots; the Sakuya slots are explicit pending placeholders while the source-derived Sakuya bomb routine metadata remains in `PlayerBombTables`. `src/EffectManager.*`, `src/Stage.*`, and other flat TH06-style runtime files still carry mixed TH07 evidence slices and need function-level replacement before gameplay parity.
- Pending: full Player shot runtime, full bomb calc/draw/update runtime, Item/Cherry/Border state transitions, GameManager/result/replay/input/window audits, ECL opcode replacement, Bullet/Laser runtime tables, and remaining renderer batch-buffer/x87 edge parity.

Latest validation checkpoint: the ECL high-opcode metadata correction through original jump table `0x00417838` has been validated with `git diff --check`, `node scripts/audit-ecl-opcodes.mjs`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug` on 2026-05-22. It remains metadata-only/source-derived, not runtime-wired interpreter behavior. The follow-up `FUN_0042d83a` score/extend-award helper and `FUN_00432990` point-item extend-threshold helper checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `node scripts/audit-ecl-opcodes.mjs`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived helper coverage only. The `FUN_00429c42` MSG opcode `0x0e` screen-fade slice was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `node scripts/audit-ecl-opcodes.mjs`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived runtime coverage for that one opcode, not full MSG VM parity. The `FUN_00439401`/`FUN_004394c7` timer increment/decrement helper checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived helper and PE-anchor coverage only, while live `ZunTimer` replacement remains blocked on Supervisor `+0x18c` flag lifetime and scheduling proof. The `FUN_00429c42` MSG opcode `0x0c`/`0x0d` checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived runtime/argument-shape coverage for those two opcodes, not full MSG VM parity. The `FUN_00427f22` bonus-toast contract checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, and `npm run audit-resources`; it is source-derived notification metadata only, not producer or ResultScreen runtime wiring. The follow-up `FUN_00427c81`/`FUN_0042adab` bonus-toast lifecycle checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, `npm run audit-diff`, and `npm run ghidra-anchors`; it is source-derived register/update lifecycle metadata only. The seven-call producer checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, and `npm run audit-diff`; it is source-derived callsite metadata only, not broader state-machine or runtime wiring. The ECL helper-call/resolved-target call-contract checkpoint was then validated with `git diff --check`, `node scripts/audit-ecl-opcodes.mjs`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is metadata-only call-target coverage for high opcodes, not interpreter runtime wiring. The Player mode active-effect lifecycle contract checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is metadata-only active-pointer lifetime coverage for `FUN_00441960`, `FUN_00441bd0`, and `FUN_00441670`, not mode producer runtime wiring. The MSG binary opcode argument-shape checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is metadata-only parser-shape coverage, not full MSG VM opcode behavior. The Timer force-step producer/read contract checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived access-contract coverage for `FUN_00418fc0`, `FUN_00439401`, and `FUN_004394c7`, not live `ZunTimer` replacement. The STD binary opcode argument-shape checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is metadata-only parser-shape coverage for fixed `0x0c` STD script argument blocks, not STD opcode runtime behavior. The ANM VM opcode argument-shape checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is metadata-only parser-shape coverage for extracted ANM VM instruction payload sizes, not additional opcode runtime behavior. The MSG dispatch/case-surface checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-diff`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived switch-table, argument-range, common-advance, and post-tick contract coverage for `FUN_00429c42`, not full MSG VM runtime parity.

Additional validation notes: the same MSG dispatch/case-surface checkpoint also passed `npm run ghidra-anchors`; the known `Invalid PNG data at 0048ef54` warning appeared with exit code 0. The follow-up Player mode update/draw branch-contract checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived dispatch/owner metadata for `FUN_00441330` and `FUN_004420b0`, not live Player runtime parity. The ECL `FUN_0040f3c0` resolved float write-target checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `node scripts/audit-ecl-opcodes.mjs`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived resolver metadata only, not live ECL interpreter wiring. The follow-up `FUN_00424c00` item-clear radius helper checkpoint used the same validation gate; it is source-derived ECL-to-Item helper metadata only, not live ItemManager or ECL runtime wiring. The paired ECL read-resolver checkpoint maps `FUN_0040e5b0` and `FUN_0040edf0` into compiled source-derived metadata and uses the same validation gate; it is still metadata-only and does not wire live ECL read semantics. The `FUN_00424740` helper checkpoint maps the opcode `146` callee surface into compiled metadata and uses the same validation gate; it is metadata-only and does not prove the broader item/object allocation lifetime. The `FUN_004326f0` checkpoint maps the 0x44c-slot visual-effect spawn contract used by ECL/item clear helpers; it remains metadata-only until the manager lifetime and update/draw consumers are proven. The Player `FUN_0043ee50` movement input/direction/focus-speed/clamp checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `node scripts/audit-ecl-opcodes.mjs`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived helper/contract coverage only, not live Player movement replacement. The `FUN_004423e0` SHT owner pointer load and `FUN_00426f60` live enemy-bullet ANM/template checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `node scripts/audit-ecl-opcodes.mjs`, `npm run ghidra-anchors`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, and `build/linux-debug/th07-vulkan --headless-smoke`; SHT owner slots remain source-derived metadata, while `BulletManager` now uses the single `data/etama.anm` load plus resolved template rows `0..9`, leaving row `10`, spawn/update/collision, and laser behavior transitional. The `FUN_00431330` FileSystem archive lookup contract checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug` on 2026-05-22; it is source-derived lookup/metadata coverage only, while the PBG3 owner load/lifetime and archive runtime remain transitional. The `FUN_00429c42` MSG opcode `0x01/0x02` portrait base checkpoint was then validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is a narrow source-derived runtime correction for portrait script/sprite bases, not full MSG VM parity.

Additional FileSystem lifecycle validation: the follow-up `FUN_00438668`/`FUN_00438de2` archive owner lifecycle checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it is source-derived owner/entry metadata and PE-checked call-site coverage only, not live PBG3 runtime replacement.

Additional FileSystem reader validation: the `Th07ArchiveReader` `PBG4` checkpoint was validated with `npm run check`, `npm run audit-cpp`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`; it decodes `reference/original/th07.dat` index metadata and selected entries against extracted originals, but `src/FileSystem.cpp` has not yet been rerouted to this reader.

Additional FileSystem runtime validation: the `Th07FileSystemRuntime` checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-diff`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, `build/linux-debug/th07-vulkan --check-resources`, and `build/linux-debug/th07-vulkan --headless-smoke`. The runtime opens the default archive, preserves the version-probe bytes/size, resolves archive paths through `ComputeTh07ArchiveLookupName`, and makes resource smoke checks read `data/text.anm` from `th07.dat`; legacy `FileSystem.cpp` and `Supervisor::LoadPbg3` still need replacement before live gameplay loaders are source-derived.

Additional STD parser validation: the `Th07StdFile` checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, `build/linux-debug/th07-vulkan --check-resources`, and `build/linux-debug/th07-vulkan --headless-smoke`. The portable parser reads all eight `reference/extracted/stage*.std` binaries through TH07 header/object/quad/instance/script sentinels, checks fixed `0x14` script records with `0x0c` argument blocks, and cross-checks parsed counts/opcode masks against `Th07ResourceFormatTables`; live `Stage::LoadStageData` still uses the transitional raw pointer-relocation path.

Additional Stage STD loader contract validation: the `FUN_00407610` checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, `build/linux-debug/th07-vulkan --check-resources`, and `build/linux-debug/th07-vulkan --headless-smoke`. `src/Th07StageLoaderTables.hpp` and `tests/test_stage_loader_contract.cpp` PE-check the original file-load, error-log, allocation, and script-setup call sites, validate owner offsets `+0x49c..+0x4b0`, raw object-offset table relocation from file `+0x490`, `declaredQuadCount * 0x24c` VM allocation sizing, object flag byte `+3 = 1`, and raw quad sentinel walks across all eight `reference/extracted/stage*.std` binaries. This is source-derived loader-contract coverage only; live `Stage::LoadStageData` still uses the transitional TH06-shaped allocation path until draw-object VM ownership/runtime behavior is proven.

Additional MSG parser validation: the `Th07MsgFile` checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, and `build/linux-debug/th07-vulkan --headless-smoke`. The portable parser reads all eight `reference/extracted/msg*.dat` binaries through the TH07 msg06 pointer table and `u16 time/u8 opcode/u8 argSize` instruction records, preserves entry-pointer aliases, and cross-checks parsed counts, opcode masks, text-byte totals, wait-frame totals, and Stage 6 opcode `0x0e` usage against `Th07ResourceFormatTables`; live `GuiImpl` still uses the transitional raw pointer-relocation path.

Additional GUI MSG loader contract validation: the `FUN_00429935`/`FUN_004299f8` checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, and `ctest --preset linux-debug`. `src/Th07GuiMsgLoaderTables.hpp` and `tests/test_gui_msg_loader_contract.cpp` PE-check the original MSG load/free call sites, `FUN_00431330` non-external file load, `FUN_004315f0` error logging through `00624210/00498108`, owner fields `+0x1fba4/+0x1fba8/+0x1fbac`, raw entry-pointer relocation from file `+4`, and `_free` cleanup. The test also walks all eight `reference/extracted/msg*.dat` pointer tables and cross-checks first-instruction headers against `Th07MsgFile`; this is source-derived loader-contract coverage only, not full `GuiImpl` runtime parity.

Additional ANM parser validation: the `Th07AnmFile` checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, and `build/linux-debug/th07-vulkan --headless-smoke`. The portable parser reads all 63 `reference/extracted/*.anm` binaries through the TH07 ANM header/sprite/script tables and `i16 opcode/u16 size/i16 time/u16 operandFlags` VM records, validates `-1` terminators, and cross-checks aggregate sprite/script totals, instruction counts, terminators, opcode usage, instruction sizes, and argument-byte shapes against `Th07ResourceFormatTables`; live `AnmManager` still needs owner lifetime, slot replacement, and dispatch proof before broad runtime replacement.

Additional ANM manager load/free contract validation: the checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, `build/linux-debug/th07-vulkan --check-resources`, and `build/linux-debug/th07-vulkan --headless-smoke`. `src/Th07AnmManagerLoadTables.hpp` records the source-derived `FUN_0044df90`/`FUN_0044e070`/`FUN_0044e4e0` contract: non-external archive load through `FUN_00431330`, the original 0x32 slot cap, version-2 header requirement, owned-file byte at header `+0x35`, chained entry walk via header `+0x38`, sprite/script global id cap `0x0a00`, manager tables at `+0x28acc/+0x28ef0/+0x2b6f0/+0x2def0`, and release-time recursive loaded-count cleanup. `tests/test_anm_manager_load_contract.cpp` PE-checks the original instructions and re-parses every manifest `RuntimeAnmLoadContract` against the extracted ANM bytes, including chained entries. This is source-derived manager contract coverage only; live `AnmManager::LoadAnm` still has the transitional TH06-shaped slot arrays until texture ownership and backend dispatch are proven.

Additional ECL parser validation: the `Th07EclFile` checkpoint was validated with `git diff --check`, `npm run check`, `npm run audit-cpp`, `npm run audit-resources`, `npm run audit-diff`, `node scripts/audit-ecl-opcodes.mjs`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, and `build/linux-debug/th07-vulkan --headless-smoke`. The portable parser reads all eight `reference/extracted/ecldata*.ecl` binaries through the TH07 header/sub-offset table, sub prologue and instruction records, and variable-size timeline records; `tests/test_ecl_parser.cpp` cross-checks 762 subs, 17 timelines, 19919 sub instructions, 762 sub terminators, 2494 timeline instructions, 16 timeline terminators, 141 unique sub opcodes, `.decl` instruction counts, and high-opcode counts against `EclOpcodeTables`. Live `EclManager::Load` still uses the transitional raw pointer-relocation path and must not be treated as source-derived runtime behavior yet.

Additional ECL manager contract validation: the `FUN_0040e420`/`FUN_0040e530` checkpoint was validated with `npm run check`, `npm run audit-cpp`, `node scripts/audit-ecl-opcodes.mjs`, `cmake --preset linux-debug`, `cmake --build --preset linux-debug`, `ctest --preset linux-debug`, `npm run audit-resources`, `npm run audit-diff`, `npm run ghidra-anchors`, `build/linux-debug/th07-vulkan --check-resources`, and `build/linux-debug/th07-vulkan --headless-smoke`. `tests/test_ecl_manager_contract.cpp` PE-checks the original loader/call-sub instruction bytes, call targets, header-relocation loop, runtime sub-offset table base, timer sentinel writes, and context sub-id write. This remains source-derived metadata only; live loader replacement is still blocked on the remaining header-offset meanings, TH07 context layout, owner lifetime, and dispatch proof.

## Confirmed Anchors

| Area | Evidence | Current Source |
| --- | --- | --- |
| Ghidra anchor xrefs | `reference/ghidra/anchors/anchor-xrefs.tsv` | `npm run ghidra-anchors` |
| Character GUI face/loading loads | `FUN_00428b19` | `src/Gui.cpp` |
| Stage face/stdtxt/msg loads | `FUN_00428b19` | `src/Gui.cpp` |
| ANM binary parser | `reference/extracted/*.anm`; `FUN_00450d60` instruction shape | `src/Th07AnmFile.*` parses all 63 extracted ANM binaries through header/sprite/script tables and VM instruction records, with aggregate metadata validated by `tests/test_anm_parser.cpp`; this is parser coverage only, not full ANM manager runtime parity. |
| MSG binary parser | `reference/extracted/msg*.dat`; `FUN_00429c42` instruction shape | `src/Th07MsgFile.*` parses all eight message binaries, preserves pointer aliases, and validates entry/instruction/opcode/text/wait summaries with `tests/test_msg_parser.cpp`; this is parser coverage only, not full MSG VM runtime parity. |
| GUI MSG loader/free contract | `FUN_00429935`, `FUN_004299f8`, `reference/extracted/msg*.dat` | `src/Th07GuiMsgLoaderTables.hpp` records the original loader/free owner fields, entry-pointer relocation, error-log path, and cleanup contract. `tests/test_gui_msg_loader_contract.cpp` PE-checks the original bytes and validates raw pointer tables across all eight MSG binaries; live `GuiImpl` remains transitional. |
| ECL binary parser | `reference/extracted/ecldata*.ecl`; `reference/ecl/ecldata*.decl`; `FUN_00410520` dispatch shape | `src/Th07EclFile.*` parses all eight ECL binaries through the header/sub-offset table, sub instruction stream, and timeline stream, and validates aggregate sub/timeline/opcode counts with `tests/test_ecl_parser.cpp`; this is parser coverage only, not live ECL interpreter parity. |
| GUI spell-card display layout | `FUN_0042868d` decompiler output and original instructions at `0042869c`, `004286af`, `004286de`, `004286f5`, `00428708`, `0042872e`, `00428750`, `00428763`, `00428789`, `004287ab`, `004287be`, `004287e9`, `004287f4`, `0042884e`, `00428857`, `00428862`, `0042886d`, and `00428879` | `src/GuiLayout.hpp` records the owner pointer at `+0x08`, the text-width field at `+0x0c`, spell-card VM offsets `+0x574c/+0x5be4/+0x607c/+0x6514`, interrupt VM offset `+0x69ac`, scripts `0x04a1/0x04a4/0x04a6/0x0704`, default name sprite `0x04ac`, text color `0x00f0f0ff`, sound `0x0e`, and global state write `00575ab4 = 2`; `tests/test_gui_layout.cpp` and `tests/test_sht_parser.cpp` verify the table and PE bytes. This is layout evidence only; the TH06-shaped `GuiImpl` runtime is not exact. |
| ASCII/capture ANM load | decompile sample around ASCII init | `src/AsciiManager.cpp` |
| Stage primary/Stage 4 extra background loads | `FUN_00407000` | `src/Stage.cpp` |
| SHT path table order and Player owner loads | xrefs through `0049f530..0049f55c`, `FUN_004423e0` entry calls to `FUN_00442b70`, and PE instruction checks at `004423e9..00442429` | `src/Th07ResourceManifest.*` owns the 12 path strings, `src/Th07PlayerShotTables.hpp` records the `0049f530/0049f548` unfocused/focused path-table contract, and `src/PlayerLayout.hpp`/`Player.hpp` expose the Player SHT pointer slots at `+0xb7e70/+0xb7e74`; `src/ShtManager.*` is only a legacy compatibility wrapper |
| SHT binary container layout | `FUN_00442b70`, `reference/extracted/ply*.sht` | `src/ShtFile.*`, `src/Th07PlayerShotTables.hpp`, `tests/test_sht_parser.cpp` |
| SHT callback table addresses | `DAT_0049ecb0`, `DAT_0049ecc8`, `DAT_0049ece0`, `DAT_0049ece8` in `th07.exe` | `src/ShtFile.*`, `tests/test_sht_parser.cpp` |
| SHT callback high-level semantics | `FUN_0043bdc0..0043d0e0` decompiler output | `ShtCallbackSemantic` in `src/ShtFile.*` |
| RNG and angle helper core | `FUN_00431870`, `FUN_004318d0`, `FUN_00431900`, `FUN_00431930`, and original `.rdata` at `00498a5c..00498b28` | `src/Rng.*` now carries the TH07 seed step, U32 composition, `2^32` float divisor, and guarded `-pi..pi` angle wrapping helpers with `tests/test_rng.cpp`. Replay seed/lifecycle consumers and caller-level x87 precision remain pending before deterministic parity can be claimed. |
| GameManager score/high-score/extend helpers | `FUN_0042d8d5`, `FUN_0042d83a`, `FUN_0042d5cd`, `FUN_0042d612`, `FUN_00432990`, and original instructions at `0042deb5`, `0042df08`, `0042df0e`, `0042df17`, `0042df26`, `0042dfd0`, `0042dfe5`, `0042dff4`, `0042dff7`, `0042d847`, `0042d859`, `0042d891`, `0042d8a3`, `004332e0`, `004333ca`, and `004335ba` | `src/Th07ScoreTables.*` carries the source-derived score clamp, GUI-score interpolation, high-score update, extend-award, and point-item extend-threshold helpers: score is clamped to `999999999`, the GUI delta is shifted by `5`, the step is capped at `0x8d55e`, a nonzero delta uses minimum step `1`, high-score replacement copies current shot byte `+0x20` into high-score shot byte `+0x10`, `FUN_0042d83a` first awards owner life stock `+0x5c` below `8` otherwise bomb stock `+0x68` below `8`, and `FUN_00432990` computes point-item life-extend thresholds as Normal-family `50,125,200,300,450,800...` and Extra-family `200,500,800...` before calling the same award helper. This is metadata/helper coverage only; the copied `GameManager.cpp` runtime still needs owner-layout and dispatch replacement before wiring. |
| ANM/STD/MSG extracted format parser coverage | `reference/extracted/*.anm`, `reference/extracted/stage*.std`, `reference/extracted/msg*.dat`, `reference/std/stage*.dstd`, `reference/msg/msg*.txt` | `scripts/audit-th07-resources.mjs`: 63 ANM raw headers with 1187 sprites and 727 scripts, plus 6214 parsed ANM VM script instructions, 727 terminators, opcode coverage including max opcode `80`, 44 aggregate ANM VM opcode usage rows, 44 ANM VM argument-size rows totaling 38724 payload bytes, and instruction sizes `8..28`; 8 STD binaries with 45 objects, 162 quads, 906 instances, 378 script instructions, 25 opcode usage rows, 25 fixed `0x0c` argument-size rows totaling 4536 argument bytes, and Stage 6 binary coverage despite `stage6.dstd` being empty; 8 readable MSG dumps with 60 entries and 1098 timeline labels; 8 MSG binaries with 180 entry pointers, 60 unique entry scripts, 4883 instructions, opcode coverage `0..14`, 15 aggregate opcode usage rows, and 15 argument-size rows totaling 46572 argument bytes. `src/Th07ResourceFormatTables.*` mirrors ANM per-file metadata plus ANM VM aggregate coverage, opcode usage, and argument-shape rows; per-stage STD binary/opcode usage/argument-shape rows; MSG dump, MSG binary summaries, MSG binary opcode usage, and MSG binary argument-shape rows with `tests/test_resource_format_tables.cpp`. `src/Th07AnmFile.*`, `src/Th07StdFile.*`, and `src/Th07MsgFile.*` now compile the three portable parsers; `tests/test_anm_parser.cpp`, `tests/test_std_parser.cpp`, and `tests/test_msg_parser.cpp` validate extracted binaries against those tables. |
| Portable STD parser and loader contract | `reference/extracted/stage*.std`, `src/Th07ResourceFormatTables.*`, `FUN_00407610` original bytes/decompiler output | `src/Th07StdFile.*` parses all eight stage binaries through the TH07 header/object/quad/instance/script sentinel layout with fixed `0x14` script records and fixed `0x0c` argument blocks. `src/Th07StageLoaderTables.hpp` records the original loader owner offsets, object-table relocation, `0x24c` VM allocation stride, active flag write, and script setup base `0x300`. `tests/test_std_parser.cpp` and `tests/test_stage_loader_contract.cpp` cross-check parsed stage data, PE bytes, and raw object/quad sentinel walks. Live `Stage::LoadStageData` still uses the transitional raw pointer-relocation path until draw-object VM ownership/runtime behavior is proven. |
| Runtime ANM string and load contracts | `reference/ghidra/strings.tsv`, `reference/extracted/*.anm`, `FUN_00401d70`, `FUN_00407000`, `FUN_0041e590`, `FUN_00426f60`, `FUN_00428b19`, `FUN_00438986`, `FUN_0043ae20`, `FUN_004423e0`, `FUN_00449b05`, `FUN_0045bf15` | `src/Th07ResourceManifest.*` now carries all 63 `data/*.anm` string anchors in original string-table order, including title/result/music/text/front/loading/player/stage/effect/face/bullet resources. It also records 46 source-derived load contracts with original ANM file slots and sprite/script offsets for ASCII/capture, GUI front/loading/face/stdtxt, stage backgrounds including Stage 4 extras, staff/text/music/result/title resources, enemy bullets, and player character ANMs. `tests/test_reference_paths.cpp` and `scripts/audit-th07-resources.mjs` cross-check these tables. The resource audit also scans live `LoadAnm` calls: 40 current calls are backed by TH07 string anchors, while 22 paths are tracked as obsolete TH06 carryover with no TH07 anchor (`staff02/03`, multi-file title/select/player-select/replay menu loads, `music01/02`, and `result01..03`). |
| FileSystem archive lookup and owner lifecycle | `FUN_00431330`, `FUN_00438668`, `FUN_00438de2`, `FUN_0045f710`, `FUN_0045f800`, `FUN_0045fae0`, `FUN_0045fab0`, `FUN_0045f960`, `FUN_0045fb50`, `FUN_0045fde0`, `FUN_0045ffc0`, original instructions at `004313a1`, `004313b1`, `004313c1`, `004313e4`, `0043141b`, `00438671`, `00438676`, `0043867b`, `004386a4`, `00438f8b`, `00438f90`, and strings at `00495084..00495100`, `00497140`, `00497150`, `00497cf8..00497d38`; `reference/original/th07.dat` and matching extracted entries | `src/Th07FileSystemTables.hpp` records the single archive owner `0x00626258`, loaded-size global `0x004b9e64`, archive find/size/read helpers, `th07.dat` open/close call sites, the generated `th07_0100b.ver` probe, owner field offsets, 0x10-byte archive-entry stride, and archive/external log string addresses. `Th07ArchiveReader` implements a portable source-derived `PBG4` header/index LZSS/data reader with case-insensitive entry lookup and decodes all 197 index entries. `Th07FileSystemRuntime` now compiles into `th07_game`, opens the default archive, preserves the version-probe bytes/size, resolves archive paths through `ComputeTh07ArchiveLookupName`, and is exercised by `--check-resources`/`--headless-smoke`; `tests/test_file_system_contract.cpp` PE-checks the owner lifecycle and compares decoded `th07_0100b.ver`/`ecldata1.ecl` against extracted originals. Legacy `src/FileSystem.cpp` still uses TH06-style multi-archive storage and remains the next replacement target. |
| Enemy bullet ANM resource and template table | `reference/ghidra/strings.tsv` string `00498524`, `FUN_00426f60`, `DAT_00495160`, `reference/original/th07.exe`, `reference/extracted/etama.anm` | `src/Th07ResourceManifest.*` anchors `data/etama.anm`; `src/Th07BulletTables.hpp` mirrors the parsed `etama.anm` header as 168 sprites, 25 scripts, 256x256, version 2, with texture data. `src/BulletManager.cpp` now loads the source-derived single `data/etama.anm` slot `0x0b` at script offset `0x200` and initializes the first 10 live bullet templates from the original `00495160` table. Slot `10` stays blocked because its scripts `0x2a8/0x2aa/0x2a9` are not present under the proven single `etama.anm` load. `tests/test_reference_paths.cpp` and `scripts/audit-th07-resources.mjs` keep the metadata in sync; copied `etama3/etama4` split-bank names remain obsolete TH06 carryover records only, not live loads. |
| ECL declaration, dispatch, and helper coverage | `reference/ecl/ecldata1-8.decl`, `reference/ghidra/decomp/functions/00410520_FUN_00410520.c`, `reference/ghidra/decomp/functions/0040e5b0_FUN_0040e5b0.c`, `reference/ghidra/decomp/functions/0040edf0_FUN_0040edf0.c`, `reference/ghidra/decomp/functions/0040f3c0_FUN_0040f3c0.c`, `reference/ghidra/decomp/functions/00424740_FUN_00424740.c`, `reference/ghidra/decomp/functions/00424c00_FUN_00424c00.c`, `reference/ghidra/decomp/functions/004326f0_FUN_004326f0.c`, `reference/ghidra/decomp/functions/0048bb40_FUN_0048bb40.c`, `reference/ghidra/decomp/functions/0048bb5d_FUN_0048bb5d.c`, `reference/ghidra/decomp/functions/0048bbf0_FUN_0048bbf0.c`, `reference/ghidra/decomp/functions/0048bc0d_FUN_0048bc0d.c`, original jump table at `0x00417838`, and original instructions | `scripts/audit-ecl-opcodes.mjs`: 141 unique script opcodes, max opcode `161`, current enum count 136, current handled count 134, 25 extracted TH07 opcodes not yet in the current `EclRawInstrOpcode` enum, and 26 dispatch gaps including NOP. `src/EclOpcodeTables.*` now mirrors the enum/dispatch gap arrays, the 25 high-opcode records, original jump-table entries/targets, 31 high-opcode operand-signature rows, 41 direct high-opcode owner write contracts, 17 helper-call/resolved-target call contracts, 70 `FUN_0040e5b0` int-read resolver contracts, 74 `FUN_0040edf0` float-read resolver contracts, 37 `FUN_0040f3c0` resolved float write-target contracts, the `FUN_00424740` clear-helper surface, the `FUN_00424c00` item-clear radius helper, the `FUN_004326f0` 0x44c-slot visual-effect spawn contract, and opcode `151` x87 projection helper contracts into compiled metadata with `tests/test_ecl_opcode_tables.cpp`; `tests/test_sht_parser.cpp` PE-checks the original call targets, read-resolver jump tables/helpers, resolver case writes, clear-helper item/object loops, visual-effect spawn allocation/init anchors, x87 projection wrappers/cores, and item clear loops. The previous `140`/`161` blocker was an off-by-one decompiler-case interpretation. Opcode `141` is source-derived as a jump directly to the common instruction-advance path. |
| Player bomb routine table addresses | `PTR_FUN_0049ec50` in `FUN_004423e0` and `th07.exe` | `src/PlayerBombTables.*`, `tests/test_sht_parser.cpp` |
| Player bomb routine slot roles | call sites at `00440b18`, `00440b26`, `004420df`, `004420ed` | `PlayerBombRoutineSlot` in `src/PlayerBombTables.*` |
| Player bomb lifecycle fields | `FUN_004409f0`, `FUN_004420b0`, `FUN_004429d0`, update dispatch around `00440b18` | player object base/clear span and bomb offsets/constants in `src/PlayerBombTables.*` |
| Player ANM VM prefix layout | `FUN_00441fb0`, `FUN_004420b0`, `FUN_004423e0`, and PE instruction checks in `th07.exe` | original `0x24c` player VM slot size, VM offsets, draw-position offsets, option-position offsets, and script indices in `src/PlayerLayout.hpp`, `src/PlayerBombTables.*`, and `Player.hpp` offset asserts |
| Player position, movement input, and collision boxes | `FUN_004423e0`, `FUN_0043ee50`, `FUN_0043e0a0`, `FUN_0043e260`, `FUN_0043e3b0`, `FUN_0043e4e0`, `FUN_0043e6b0`, `FUN_00440940`, `FUN_00441800`, `FUN_004418b0`, and PE instruction checks in `th07.exe` | original position, movement delta, direction state `+0x241c`, previous/current speed fields `+0x2420/+0x2424`, focus byte `+0x240b`, hitbox, graze box, grab-item box, size-vector, option-position offsets, shot-data speed offsets `+0x24/+0x28/+0x2c/+0x30`, playfield clamp globals `0062f874/0062f878/0062f87c/0062f880`, and horizontal ANM transition scripts `0x401..0x404` in `src/PlayerLayout.hpp`, `Player.hpp`, and tests. The shared bomb collision slot path is now mapped at `+0x17dc`, with 96 slots of stride `0x20`, raw payload bits at `+0x1c`, `Player::CalcBombCollision`, `Player::UpdateBombCollisionSlots`, `Player::RegisterBombCollisionRect`, and `Player::RegisterBombCollisionCircle` all present as comparable Player.cpp surfaces. `Player::AddedCallback` now seeds SHT-derived hit/graze sizes, `Player::HandlePlayerInputs` live-bridges the direction/focus/speed/delta/clamp/collision-box core through the raw TH07 owner offsets, `Player::CalcItemBoxCollision` uses the source-derived `FUN_0043e4e0` raw mode-state gate plus half-size item overlap, and live `Player::CheckGraze` now uses the original `+0x0960/+0x096c` graze box through the source-derived `20.0f` margin helper. Kill/graze side-effect parity, laser helper parity, live bomb producer replacement, SakuyaB's special option orbit/effect pointer branch, and the shot-dispatch tail remain pending, so full Player movement/collision parity is not yet claimed. |
| Player SHT shot spawn/update/draw/timer/damage surfaces | `FUN_0043d160`, `FUN_0043d2f0`, `FUN_0043d690`, `FUN_0043d790`, `FUN_0043d880`, `FUN_0043d990`, `FUN_0043d9e0`, `FUN_0043bdc0`, `FUN_0048b8a0`, `FUN_00431930`, `FUN_0042d6d8`, `FUN_0044ea20`, `FUN_0044f9a0`, `FUN_00450d60`, SHT power table records, and matching decompiler output | `Player::SpawnBullets` now has a diff-only source-derived surface that follows the original focus byte `+0x240b`, SHT owner pointers `+0xb7e70/+0xb7e74`, power-row scan from SHT `+0x34`, raw 96-slot shot array at `+0x2444` with stride `0x364`, free-state word `+0x34a`, render flag bit `0x1000`, shot-record pointer `+0x360`, callback tail writes `+0x354/+0x358/+0x35c`, default spawn callback `FUN_0043bdc0`, and x87 `FUN_0048b8a0` power truncation. `Player::UpdatePlayerBullets` now has the adjacent diff-only raw update surface for option tracked-shot cleanup at `+0x169c4/+0x169d0`, update callback dispatch from slot `+0x354`, movement through `+0x24c/+0x250` and `+0x324/+0x328`, bounds checks through `FUN_0042d6d8`, ANM VM stepping through `FUN_00450d60`, and timer stepping through `FUN_0043958d`. `DrawBullets`/`DrawBulletExplosions` now draw active/collided raw slot states `1/2`, normalize auto-rotation through `FUN_00431930`, apply draw offsets `0062f864/0062f868`, force draw `z = 0.4f`, call `FUN_0044f9a0`, and dispatch the per-shot draw callback for active slots. `StartFireBulletTimer`/`UpdateFireBulletsTimer` now expose the original timer at `+0x169f4/+0x169f8/+0x169fc`, replay-input gate globals `004d44f8/0062f645/0062f646`, spawn dispatch, TH07 timer tick helper, and mode-state cancellation. `CalcDamageToEnemy` now exposes the source-derived damage pass over raw shot slot sizes/damage/state/type/collision callback fields, laser parity gating, collided-state conversion through `FUN_0044ea20` plus effect id `5`, velocity slowdown by the original `00498a7c` divisor, and the 112-entry bomb-region damage/accumulation/effect path at `+0x09dc/+0x09e8/+0x09f4/+0x09f8`. Live gameplay still uses the transitional `PlayerBullet` path until the raw TH07 shot-slot layout replaces it. |
| Player option-shot effect callback | `FUN_0043d0e0`, `FUN_0041c1c0`, and `reference/ghidra/decomp/functions/0043d0e0_FUN_0043d0e0.c` | `Player::OptionShotEffectCallback` now exposes a comparable source surface for the SHT collision callback that increments player byte `+0x240c`, gates effect creation every 8 hits, copies impact `y/z`, replaces effect `x` with the shot slot's `+0x24c` position, and spawns effect id `5` through the TH07 effect manager at `0x012fe250`/`FUN_0041c1c0` with count `1` and color `0xffffffff`. The pure helper covers counter wrap and spawn-vector behavior; live callback dispatch remains pending until the TH07 shot-slot layout fully replaces the transitional `PlayerBullet` shape. |
| Player mode-1 invulnerable update | `FUN_004411c0`, `.rdata` constants `00498a54/00498a70/00498b00`, and `reference/ghidra/decomp/functions/004411c0_FUN_004411c0.c` | `Player::UpdateMode1Invulnerable` now exposes a comparable source surface for sound timer `+0x2400`, primary VM scale/color/blend writes at `+0x18/+0x1c/+0x1b8/+0x1c0`, movement multiplier resets `+0x23f0/+0x23f4`, transient `+0x23f8` clearing, 30-frame fade/alpha math, completion mode write `+0x2408 = 3`, common-effect timer reset `+0x16a00/+0x16a04/+0x16a08`, and the unresolved `DAT_00575948 + 8` cap reload. Runtime wiring remains pending until that owner pointer and the mode dispatch call order are identified. |
| Player option-field reset | `FUN_00441e80`, `th07.exe` `.rdata` constants `00498afc/00498ac0`, and `reference/ghidra/decomp/functions/00441e80_FUN_00441e80.c` | `Player::ResetOptionFields` now exposes a comparable source surface for the original reset writes at `+0x2428..+0x2440`, including `-999.0f` sentinel raw bits, zero z/aux fields, threshold constants `400.0f` and `160.0f`, and the `013542ec`/`0134d476` pending-state transition. The mirror global is recorded as a 16-bit write. Runtime wiring remains pending until those global owners are identified. |
| Player objdiff readiness | `reference/original/th07.exe`, `reference/ghidra/functions.tsv`, `config/player_function_manifest.csv`, and `objdiff-cli` | `scripts/objdiff-player.mjs` now builds a generated original object for all 34 original Player-range functions in `0x0043d0e0..0x004429d0`, compiles diff-only current 32-bit objects for `src/Player.cpp`/`src/PlayerBombTables.cpp` with i486/x87-oriented flags, and compares the 28 functions that currently have source symbols. The checked baseline now includes `FUN_0043d0e0` at `30.48%`, `FUN_0043d160` at `27.79%`, `FUN_0043d2f0` at `16.59%`, `FUN_0043d690` at `32.76%`, `FUN_0043d790` at `9.61%`, `FUN_0043d880` at `44.47%`, `FUN_0043d990` at `37.93%`, `FUN_0043d9e0` at `21.78%`, keeps `FUN_0043e4e0` at `30.47%`, keeps `FUN_004411c0` at `32.32%`, keeps `FUN_00441e80` at `36.22%`, includes the bomb-slot helpers `FUN_00440940` at `30.17%`, `FUN_00441800` at `32.58%`, and `FUN_004418b0` at `32.80%`, and keeps older file-level comparisons such as `FUN_0043eb90` at `14.78%`, `FUN_00442350` at `42.17%`, and `FUN_00442370` at `32.32%`. This proves file-level comparability and one behavior-matched item-box function, not full Player parity. |
| Player bomb start constants | `FUN_004083f0`, `FUN_004084f0`, `FUN_00408610`, `FUN_00408710..0040e280`, plus the `004083f0`/`00441330`/`004420b0` calls into `FUN_00406930`, decompiler output and PE instruction checks in `th07.exe` | duration/common-effect/score-drain/speed/backdrop/stage-color fields and helpers in `src/PlayerBombTables.*`; `Player.hpp` carries `+0x16a00..+0x16a14`; `DAT_004d44e0` is mapped as `DAT_004bdad8 + 0x16a08` |
| Player mode transition effect metadata | `FUN_00441960`, `FUN_00441bd0`, `FUN_00441670`, and PE instruction checks in `th07.exe` | raw mode/request values, active effect pointer offsets, `0x1c` effect spawn metadata, mode-4/mode-3 durations, scale constants, and the structured active-effect lifecycle contracts in `src/PlayerBombTables.*` |
| Player mode update/draw branch contracts | `FUN_00441330`, `FUN_004420b0`, and PE instruction checks in `th07.exe` | `PlayerModeRuntimeBranchContract` records the mode-3 update, mode-4 update, default update, and draw-time mode-4 dispatch branches with timer calls, active-pointer slots, duration gates, blink color writes, cleanup call, and stage-color producer calls in `src/PlayerBombTables.*` |
| Effect table and manager layout | `FUN_0041c1c0`, `FUN_0041c400`, `FUN_0041c610`, `FUN_0041c790`, `FUN_0041cde0`, `0x0049efc0..0x0049f158` in `th07.exe` | 34-entry metadata, slot/layout constants, dedicated-spawn contract, draw-list offsets, stage effect ANM load table, and TH07-shaped `src/Effect.hpp`/`src/EffectManager.*` skeleton |
| TH07 draw/shake/color path | `FUN_0044f9a0`, `FUN_00450130`, `FUN_004504b0`, `FUN_0044efb0`, `FUN_0044b0e0`, `FUN_00406930`, `FUN_004069d0`, `FUN_00406de0`, and PE checks around `DAT_0062f858` | `src/AnmManager.*`, `src/ScreenEffect.cpp`, `src/Stage.*`, `src/Th07EffectTables.*` |
| Clear bonus constants/rank labels/owner fields | `FUN_00427f22` decompiler output and original instructions at `00427f45`, `00427ffb`, `0042803a`, `00428079`, `0042831a`, `00428368`, `00428381`, `00428392`, `004283b2`, `004283bb`, `004283c1`, `004283c7`, `004283d1`, `004283f5`, `00428414`, `00428428`, `0042843c`, `00428471`, `0042848a`, `004284b1`, `004284c5`, `004284df`, `004284e5`, `0042851a`, `0042852a`, `00428565`, `00428577`, `004285bf`, plus formula instructions at `00427fc2`, `00428001`, `00428040`, `0042807f`, `00428103`, `00428143` and string anchors `0049838c..00498518` | `src/ClearBonusSystem.*` is now compiled as a portable helper independent of `GameManager.hpp`: stage, point-item, graze, cherry, life, and bomb clear-bonus formulas, six rank labels/multipliers, four bonus-toast rules for kind `1..4`, and 15 Result/GUI owner fields from `+0x209b4` through `+0x20a2c` are covered by `tests/test_clear_bonus.cpp`; `tests/test_sht_parser.cpp` PE-checks the original instruction, owner-field, color, render-mode, and string anchors. ResultScreen owner lifetime/dispatch is still pending before runtime wiring. |
| Cherry/Border notification labels and toast lifecycle | `FUN_00427c81`, `FUN_00427f22`, `FUN_0042adab`, `FUN_00428b19`, producer calls from `FUN_0042f5a2`, `FUN_0042f69f`, `FUN_00432990`, `FUN_00441670`, and `FUN_00441960`, `reference/ghidra/strings.tsv`, and original strings at `004983a0`, `004983b4`, `004983c8`, `004983e0`, `00498410`, `00498424`, `00498438`, and `004984cc` | `src/CherrySystem.*` and `src/BorderSystem.*` carry typed string-address anchors, while `src/ClearBonusSystem.*` carries the source-derived notification contracts. `FUN_00427c81` registers position `+0x209e0`, score `+0x209ec`, kind `+0x209f0`, and timer `+0x209f4/+0x209f8/+0x209fc` with previous `-999`; `FUN_00427f22` draws kind `1..4`; `FUN_0042adab` slides for 30 frames, expires at 180 frames, clears kind, and ticks the timer; `FUN_00428b19` initializes kind to zero. Producer callsites are mapped as 2 CherryPoint Max calls, 3 Full Power calls, 1 Border Bonus call, and 1 Supernatural Border call. `FUN_0042f5a2`/`FUN_0042f69f` now also have source-derived Cherry gain contracts over manager `0x00626270`: both increment/clamp `+0x961c` against `+0x9618` and emit the CherryPoint Max toast when the cap is newly reached; the primary helper also increments/clamps `+0x9620` against `*(owner+8)+0x88 + 50000`, gates on `DAT_004bfee5`, and calls `FUN_00441960`. Broader Cherry/Border state transitions remain pending. |
| TH06-style source layout | GensokyoClub/th06 source tree shape | flat `src/` plus `src/pbg3/` |
| Modern project shell | local CMake/SDL3/Vulkan scaffold | `src/Th07App.cpp` |
| Full decompiler corpus | `reference/ghidra/decomp/summary.txt`: 1574/1574 functions | `npm run ghidra-decomp` |
| Portable resource manifest | Ghidra string addresses and TH07 route tables | `src/Th07ResourceManifest.*` |

## Not Yet 100% Synchronized

| Area | Why Not Done |
| --- | --- |
| Player shot behavior | `.sht` binary records plus SHT/player callback table addresses, high-level callback semantics, 12-file SHT shot summaries, init-time Player SHT pointer slots `+0xb7e70/+0xb7e74`, the original `0x24c` ANM VM prefix layout, the position/collision field slice, raw movement input/direction/focus-speed/clamp contract, and the raw mode/option bytes are decoded. `Player.hpp` now carries the leading TH07 layout bridge and `Player.cpp` seeds the low-byte mode/option/request initial state. `Player::SpawnBullets`, `Player::UpdatePlayerBullets`, `Player::DrawBullets`, `Player::DrawBulletExplosions`, `Player::StartFireBulletTimer`, `Player::UpdateFireBulletsTimer`, `Player::CalcDamageToEnemy`, and `Player::OptionShotEffectCallback` now expose source-derived comparable surfaces for the SHT spawn/update/draw/timer/damage/collision paths, but live runtime still depends on the transitional `PlayerBullet` layout until the original `0x364` shot slots, update/draw/collision callbacks, and firing dispatch are safely wired. |
| Bomb behavior | TH07 focused/unfocused bomb routine slots, lifecycle fields, player object base/clear span, player ANM VM prefix layout, position/collision fields, raw mode/option fields, common-effect durations, common start-effect spawn metadata, score-drain factors/minimums, score-drain-step math, common backdrop color timing/runtime color-write wiring, mode-4 stage-color write producers, mode transition effect metadata, active-effect lifecycle contracts, mode update/draw runtime branch contracts, and movement speed multipliers are decoded. The `+0x23f0/+0x23f4` speed multiplier offsets, low-byte mode/option/request initialization, `+0x16a00..+0x16a14` common-effect tail, and `+0xb7e68/+0xb7e6c` active-pointer slots are now structurally placed; the live bomb table covers six shot types but Sakuya runtime slots remain explicit pending placeholders; mode/common-effect producers still need runtime dispatch and owner mutation wiring. |
| Effect manager | TH07's 34-entry effect table, slot size/counts, dedicated-spawn contract, draw-list routing, stage effect ANM loads, first callback slices, stage camera model, Stage ANM VM runtime slice, render-helper routing, `FUN_0044efb0` submit slice, `FUN_0044b0e0` shake draw offsets plus the mapped `DAT_0062f858` GameManager frame-counter gate, and the `FUN_00406930`/`004069d0`/`00406de0` color-multiplier path are anchored in portable metadata and PE/audit checks. The common bomb backdrop producer `004083f0` is wired through the Stage color writer and its `_DAT_0134cdb0 = 1` reset guard is mapped to `DAT_01347b00 + 0x52b0`; the mode transition active-effect lifecycle is now structured as metadata, while the mode-4 player producers and transition effect runtime still need source-derived dispatch wiring. Several TH07 callback bodies, exact batched `FUN_0044f690`/`FUN_0044f5c0` buffer flushing, exact x87/CRT edge-case parity for math/float comparisons, and the conditional RGB-white gate for `0041b0b0`/`0041b4a0` still need function-level replacement. |
| Item manager | Cherry, Cherry+, Border start/break/item scoring transitions are not fully reconstructed. `FUN_00424c00` is source-derived as the ECL opcode `143` helper that scans `DAT_0063b218`, filters item state `!= 0 && != 5`, compares against a radius around an enemy position, spawns effect kind `6` arg `1`, and clears the item slot. `FUN_00424740` also scans the same 0x400 item slots, marks/clears them based on its mode argument, and then traverses a separate 0x40-entry owner-relative object array at `+0x366628`; both helpers now point at the mapped `FUN_004326f0` visual-effect spawn contract. Broader item allocation/lifetime and collection behavior still need replacement. |
| GameManager layout | TH06 layout assert is disabled; TH07 field offsets need mapping before restoring asserts. The `DAT_0062f858` frame counter is mapped as `DAT_00626270 + 0x95e8`, and the `FUN_0042d8d5` GUI-score clamp/step plus high-score update cores are source-derived in `Th07ScoreTables`. `FUN_0042d83a` now proves the life/bomb extend-award path against owner fields `+0x5c/+0x68`, and `FUN_00432990` proves point-item extend thresholds through owner fields `+0x28/+0x2c/+0x30`. The copied TH06 static score-threshold array is not part of this TH07 path and must remain obsolete carryover unless a separate TH07 source is found. |
| ResultScreen layout | Clear bonus constants, rank labels, bonus-toast kind/register/update rules, and `FUN_00427f22` owner-field reads/writes are now compiled and PE-checked as source-derived helpers, but the full score-file/result-screen structure still inherits TH06 code and has not been wired to the owner offsets. |
| RNG/replay consumers | Core helper functions and constants are source-derived and compiled, but the replay seed lifecycle, caller-level x87/float storage behavior, and all gameplay consumers still need evidence-backed parity checks before deterministic replay behavior is exact. |
| Timer/frame stepping | `FUN_0043958d`, `FUN_0043a0d6`, and `.rdata` constants `00498a54 = 1.0f`, `00498a58 = 0.99f` prove the source-derived tick rule now carried by `src/Th07Timer.hpp`: original `DAT_00575ac8` is `Supervisor +0x178`, and that same timer multiplier drives both the `> 0.99f` whole-frame path and the `<= 0.99f` subframe accumulator path. `FUN_00439401` and `FUN_004394c7` now source the timer increment/decrement helper surface, including the force-step flag read at `DAT_00575adc` (`Supervisor +0x18c`, bit 5), `previous = -999`, zero-subframe writes, negative-value cross-calls, and the zero/one-frame thresholds at `00498a4c/00498a54`. `FUN_00418fc0` now anchors the known bit-5 producer after deriving the timer multiplier; `tests/test_sht_parser.cpp` PE-checks the producer read/or/write instructions at `004190bb/004190c1/004190c4` plus the increment/decrement read sites. No direct bit-5 clear has been proven yet in the mapped decompiler output, so remaining Supervisor `+0x18c` flag bits/reset semantics remain pending. `Th07EffectTables` uses the tick helper at the current portability boundary. Live `ZunTimer` replacement, frame scheduling, replay/input consumers, and the remaining Win32/D3D include boundary still need focused evidence before deterministic timing parity can be claimed. |
| ECL interpreter | Resource manifests are TH07 and extracted script opcode coverage is now audited across all eight `.decl` dumps. `src/Th07EclFile.*` parses all eight original ECL binaries and validates 762 subs, 17 timelines, 19919 sub instructions, 2494 timeline instructions, 141 unique sub opcodes, `.decl` instruction counts, and high-opcode counts. `src/Th07EclManagerTables.hpp` now records the original `FUN_0040e420` loader contract: 16 header-offset relocations from file `+0x04..+0x40`, runtime sub table at `file + 0x44` skipping the sentinel at `0x40`, and `subCount` relocated entries. `FUN_0040e530` proves the call-sub current-instruction, timer sentinel, and sub-id writes through context `+0x214`. Live `EclManager::Load` still uses the transitional raw pointer-relocation path because remaining header-offset meanings, TH07 context layout, and owner lifetime are not fully proven. Current evidence shows 141 unique script opcodes through opcode `161`; opcodes `136..146`, `148..161` are present in TH07 scripts but missing from the copied enum/dispatch. `src/EclOpcodeTables.*` now mirrors the current enum/dispatch coverage boundary, the missing-enum and missing-dispatch opcode arrays, the 25 high-opcode case surfaces, `opcode - 1` jump-table targets, the 31 observed high-opcode operand signatures, 41 source-derived direct owner write contracts covering enemy fields, global tables, pointer-table entries, timer resets, bit writes, and one block copy, plus 17 helper-call/resolved-target contracts for opcodes `138`, `143`, `146`, `151`, `154`, `155`, `159`, and `160`. The paired read resolver tables now cover 70 `FUN_0040e5b0` int-read contracts and 74 `FUN_0040edf0` float-read contracts across enemy fields, global fields, RNG helpers, vector-length helper, and global-position distance helper. The `FUN_0040f3c0` resolver target table adds 30 enemy-field and 7 global float write targets used by resolved-target cases; `FUN_00424740` adds the opcode `146` clear-helper surface; `FUN_00424c00` adds the item-clear radius helper surface for opcode `143`; `FUN_004326f0` adds the shared visual-effect spawn contract used by the mapped ECL/item clear paths; opcode `151` now records the x87 sine/cosine wrappers `0048bb40`/`0048bbf0`, their cores `0048bb5d`/`0048bc0d`, finite-check helper `00483d38`, range-reduction constant `004911ba`, and math-error contexts; `FUN_00455170` adds the opcode `138` record-builder helper that writes two interleaved lanes at `enemy+0x39f8` from `enemy+0x1e4` metadata; opcode `160` now points at the source-derived Cherry primary gain helper contract. These cases remain metadata-only until owner lifetimes and remaining helper callees are proven. Opcode `141` currently has no decompiler case body because its jump-table target is the common instruction-advance path. |
| Bullet/laser manager | The enemy bullet ANM resource is now source-derived as single `data/etama.anm` evidence, and `BulletManager` live loading no longer uses copied `etama3/etama4` split banks. `DAT_00495160` proves 11 bullet template script rows; slots `0..9` are now used for live template setup with extracted `etama.anm` active sprites and `FUN_00426f60` graze/category constants. Slot `10` remains blocked because it references unresolved script ids `0x2a8/0x2aa/0x2a9`; bullet spawn/update/collision runtime and laser behavior still need executable-backed owner/layout replacement before broader `BulletManager` parity can be claimed. |
| ANM/STD/MSG/ECL parsers | Core extracted-resource parser checks are present. The compiled metadata now includes all 63 ANM file summaries, aggregate ANM VM script-instruction coverage, 44 ANM VM opcode usage rows, 44 ANM VM opcode argument-shape rows, all eight STD binary summaries, 25 STD opcode usage rows, 25 STD opcode argument-shape rows, all eight MSG dump summaries, all eight MSG binary summaries, 15 MSG binary opcode usage rows, and 15 MSG binary opcode argument-shape rows, each cross-checked by `scripts/audit-th07-resources.mjs`. `src/Th07AnmFile.*` compiles the ANM binary parser over raw header/sprite/script tables and `i16 opcode/u16 size/i16 time/u16 operandFlags` instructions followed by `-1` terminators; opcode `3` is currently the highest-frequency ANM VM opcode with 1769 instructions and fixed 4-byte payloads, while opcode `32` has the largest aggregate payload at 4420 bytes. Exact ANM runtime semantics remain limited to the already source-derived Stage ANM VM opcode slice. `src/Th07StdFile.*` compiles the STD binary parser over fixed `0x14` byte records with fixed `0x0c` argument blocks across all 378 parsed instructions. `src/Th07MsgFile.*` compiles the MSG binary parser over `u32 entry count`, `u32 entry offsets`, then `u16 time/u8 opcode/u8 argSize` instructions with opcodes `0..14`; opcode `5` is currently the highest-frequency MSG opcode with 1684 instructions, opcodes `3` and `8` are variable-size text-like instructions (`8..48` and `24..40` bytes), and Stage 6 carries the three observed opcode `14` screen-fade instructions. `src/Th07EclFile.*` compiles the ECL binary parser over the `0x40` header/table, sub prologue and instruction records, and variable-size timeline records across all eight original binaries; parser tests cross-check `.decl` instruction counts and high-opcode counts. `FUN_00429c42` now provides the source-derived dispatch table and common advance/post-tick contract for all 15 MSG opcodes, while exact runtime actions remain limited to the already sourced opcode `1` portrait script base, opcode `2` portrait sprite base, opcode `12` music fadeout, opcode `13` wait-skippable write, and opcode `14` full-screen white fade trigger used by `msg6.dat`. The current readable STD dump coverage has a known Stage 6 gap: `reference/std/stage6.dstd` is empty; the binary audit and `src/Th07ResourceFormatTables.*` cover `reference/extracted/stage6.std` directly with 2 objects, 4 quads, 36 instances, 58 script instructions, and opcodes `0,1,3,4,5,6,7,9,11,29,30,31`. |
| GUI runtime | `FUN_0042868d` now proves the spell-card display VM offsets and setup constants, `FUN_00428b19` proves the face/loading/stdtxt/msg resource loads, and `FUN_00429c42` proves the 15-entry MSG dispatch table, argument-size ranges, common instruction-advance path, timer tick call, and post-tick ANM VM path. Opcodes `0x01/0x02` now use TH07 portrait script/sprite bases and opcodes `0x0c..0x0e` also have source-derived runtime actions: music fadeout, wait-skippable byte write, and full-screen white fade. The live `GuiImpl` structure and broader draw/update dispatch still carry TH06-shaped assumptions; full owner allocation lifetime, remaining MSG opcode behavior, VM slot replacement, and Result/Cherry/Border integration remain pending. |
| ANM index table | The 63 runtime ANM paths, Ghidra string addresses, and 46 source-derived load slot/offset contracts are now in `Th07ResourceManifest`. `Th07AnmManagerLoadTables.hpp` adds the original manager-side load/free contract: 0x32 slot cap, chained-entry count, owned-file flag, sprite/script index cap, and slot/script/texture table offsets. `scripts/audit-th07-resources.mjs` identifies the 22 live `LoadAnm` paths with no TH07 string anchor as obsolete TH06 carryover. The live `AnmIdx.hpp` still has obsolete TH06 title/select/result/music/bullet slot names and offsets. Replace those only after each original load slot and sprite/script offset contract is proven and texture/backend lifetime is mapped. |
| Replay/input/window systems | Not yet audited against TH07 functions. |
| Portable engine migration | Deterministic logic still needs replacement of Win32/D3D8-style reconstruction code with portable TH07 behavior in the flat `src/` tree. |

Next proof step: use the compiled ANM parser plus ANM manager load/free contract as the loader replacement baseline only after texture ownership and backend dispatch are proven; use the compiled STD/MSG/ECL parsers as safe loader replacement baselines only after each owner lifetime/dispatch contract is proven. Other high-value proof steps remain: prove caller ordering and owner lifetime around the Cherry/Border state transitions before wiring `FUN_0042f5a2`/`FUN_0042f69f`; prove the opcode `138` `enemy+0x39f8` record owner lifetime before wiring `FUN_00455170`; prove the `FUN_004326f0` visual-effect manager lifetime/update/draw consumers before runtime wiring; use the bonus-toast slice only after the owning Result/GUI allocation lifetime, broader Cherry/Border state transitions, and dispatch path are proven; replace the copied score/extend runtime only after the `FUN_00432990` item-manager owner allocation and caller path are mapped; or use the now-carried Player active-effect lifecycle contract to prove mode-4/mode-3 dispatch ordering and owner mutation side effects before wiring the mapped `FUN_00406930` producers. In parallel, decide whether the remaining renderer migration should reconstruct the original `FUN_0044f690`/`FUN_0044f5c0` batch buffer or keep the current immediate diffuse submission as a portability boundary; `DAT_004d44e0` is known to be the original global-player `+0x16a08` alias.
