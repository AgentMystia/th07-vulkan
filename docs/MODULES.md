# Module Map

| TH07 Module | Current Source | TH06 Template | Status |
| --- | --- | --- | --- |
| `Rng` | `src/Rng.*` | TH06 `Rng.*` | Source-derived: TH07 seed step, U32 composition, float divisor, and angle normalization helper addresses/constants are compiled and tested; replay/gameplay consumer parity pending |
| `ZunTimer` / `Th07Timer` | `src/ZunTimer.*`, `src/Th07Timer.hpp` | TH06 `ZunTimer.*` | Source-derived helper: `FUN_0043958d` proves the frame/subframe tick rule, `DAT_00575ac8` as `Supervisor +0x178`, and constants at `00498a54/00498a58`; `FUN_00439401`/`FUN_004394c7` prove increment/decrement, `DAT_00575adc` bit 5 as the force-step flag read surface, the `00498a4c` zero threshold, and the `-999` previous sentinel. `FUN_00418fc0` now anchors the known bit-5 producer after deriving `DAT_00575ac8`, and `Th07Timer.hpp` carries the producer/read access contract. `Th07EffectTables` currently uses the tick helper at the portability boundary. Live `ZunTimer` replacement, remaining Supervisor `+0x18c` bits/reset semantics, frame scheduling, replay/input consumers, and the remaining Win32/D3D include boundary are still pending. |
| `FileSystem` / archive lookup | `src/FileSystem.*`, `src/Th07FileSystemTables.hpp`, `src/Th07ArchiveReader.*`, `src/Th07FileSystemRuntime.*` | TH06 `FileSystem.*` | Transitional: `FUN_00431330` proves TH07's archive lookup contract, single archive owner `0x00626258`, last-size global `0x004b9e64`, archive miss log path, and basename edge cases. `FUN_00438668`, `FUN_00438de2`, `FUN_0045f710`, and `FUN_0045f800` prove the `th07.dat` owner open/close lifecycle, owner field offsets, and 0x10-byte archive-entry stride. `Th07ArchiveReader` parses the original `PBG4` archive, and `Th07FileSystemRuntime` now compiles into `th07_game`, opens the default archive/version probe, and resolves non-external paths through the TH07 basename contract. Legacy `FileSystem.cpp`/`Supervisor` still carry copied multi-PBG3 storage until startup/menu/stage ownership is replaced |
| `AnmManager` | `src/AnmManager.*`, `src/Th07AnmManagerLoadTables.hpp` | TH06 `AnmManager.*` | Same parser family, transitional runtime: compiled ANM parser coverage and the source-derived `FUN_0044df90`/`FUN_0044e070`/`FUN_0044e4e0` load/free slot contract now exist separately, but live manager loading/execution still needs texture owner lifetime, slot replacement, backend dispatch, and opcode dispatch proof before broad replacement |
| `Th07AnmFile` | `src/Th07AnmFile.*` | TH06 `AnmManager.*` raw ANM reads | Source-derived portable parser: all 63 `reference/extracted/*.anm` binaries parse through TH07 header/sprite/script tables and `i16 opcode/u16 size/i16 time/u16 flags` VM instructions with `-1` terminators; `tests/test_anm_parser.cpp` cross-checks sprite/script totals, texture metadata, opcode usage, instruction sizes, argument-byte shapes, `etama.anm`, `player02.anm`, and `capture.anm` against `Th07ResourceFormatTables` |
| `Th07StdFile` / Stage STD loader | `src/Th07StdFile.*`, `src/Th07StageLoaderTables.hpp` | TH06 `Stage.*` raw STD reads | Source-derived portable parser plus loader contract: all eight `reference/extracted/stage*.std` binaries parse through TH07 header/object/quad/instance/script sentinels, fixed `0x14` script records, and fixed `0x0c` argument blocks; `FUN_00407610` now proves the owner offsets, raw object-table relocation, `0x24c` quad VM allocation stride, object active flag write, and script setup calls with base `0x300`. `tests/test_std_parser.cpp` cross-checks parsed counts, opcode masks, Stage 6 offsets, and song strings against `Th07ResourceFormatTables`; `tests/test_stage_loader_contract.cpp` PE-checks the original loader and validates raw object tables across all eight stage binaries |
| `Th07MsgFile` | `src/Th07MsgFile.*` | TH06 `Gui.*` raw MSG reads | Source-derived portable parser: all eight `reference/extracted/msg*.dat` binaries parse through the TH07 msg06 pointer table and `u16 time/u8 opcode/u8 argSize` instruction stream; `tests/test_msg_parser.cpp` cross-checks entry-pointer aliases, unique entries, instruction counts, opcode masks, text-byte totals, wait-frame totals, and Stage 6 opcode `0x0e` usage against `Th07ResourceFormatTables` |
| `Th07EclFile` | `src/Th07EclFile.*` | TH06 `EclManager.*` raw ECL reads | Source-derived portable parser: all eight `reference/extracted/ecldata*.ecl` binaries parse through the TH07 0x40-byte header/sub-offset table, sub prologues, `i16 opcode/u16 size/i16 time/u16 operandMask` records, and variable-size timeline records; `tests/test_ecl_parser.cpp` cross-checks 762 subs, 17 timelines, 19919 sub instructions, 2494 timeline instructions, 141 unique sub opcodes, `.decl` instruction counts, and high-opcode counts against `EclOpcodeTables` |
| `GuiLayout` / `Gui` MSG | `src/GuiLayout.hpp`, `src/Th07GuiMsgLoaderTables.hpp`, `src/Gui.*` | TH06 `Gui.*` | Source-derived metadata/runtime slice: `FUN_0042868d` spell-card display VM offsets, scripts, text-width field, pending interrupt, sound, and global state are compiled and PE-checked; `FUN_00429935`/`FUN_004299f8` now prove the MSG loader/free contract including owner offset `+0x1fba4`, current-instruction/index initialization, raw entry-pointer relocation, and error logging; `FUN_00429c42` MSG now has the full 15-entry switch table, argument-size ranges, common advance path, and post-tick ANM VM path recorded and PE-checked. Opcodes `0x01/0x02` now use the TH07 portrait script/sprite bases including the enemy sprite offset `0x0d`, and opcodes `0x0c..0x0e` have source-derived runtime actions; broader `GuiImpl` runtime replacement and remaining opcode semantics are pending |
| `Stage` | `src/Stage.*`, `src/Th07StdFile.*`, `src/Th07StageLoaderTables.hpp` | TH06 `Stage.*` | Changed: TH07 stage1-8 manifest applied; portable STD parser and `FUN_00407610` loader contract are source-derived and tested. Live `Stage::LoadStageData` still uses transitional raw pointer relocation and an obsolete `sizeof(AnmVm)` quad VM allocation shape; replace it only after the remaining draw-object VM ownership and runtime behavior are proven |
| `EclManager` | `src/EclManager.*`, `src/EclOpcodeTables.*`, `src/Th07EclFile.*`, `src/Th07EclManagerTables.hpp` | TH06 `EclManager.*` | Source-derived parser/metadata, transitional runtime: `Th07EclFile` validates all eight ECL binaries and binary high-opcode counts; `FUN_0040e420`/`FUN_0040e530` now prove the TH07 manager load/call-sub contract, including 16 header offset relocations, runtime sub-table pointer `file + 0x44` that skips the file sentinel at `0x40`, and context timer/subId writes through `+0x214`. Extracted `.decl` coverage, missing enum/dispatch gaps, high-opcode operand signatures, corrected `00410520` `opcode - 1` jump-table targets, 41 direct owner write contracts, 17 helper-call/resolved-target call contracts, 70 `FUN_0040e5b0` int-read resolver contracts, 74 `FUN_0040edf0` float-read resolver contracts, 37 `FUN_0040f3c0` resolved float write-target contracts, `FUN_00424740`, the `FUN_00424c00` item-clear radius helper, the `FUN_004326f0` visual-effect spawn contract, opcode `151` x87 projection helper wrappers/cores (`0048bb40`/`0048bbf0` via `0048bb5d`/`0048bc0d`), and the `FUN_00455170` opcode `138` record-builder helper are compiled and audited; runtime loader/opcode replacement pending owner/lifetime, remaining header-offset naming, and helper-callee proof |
| `EnemyManager` | `src/EnemyManager.*` | TH06 `EnemyManager.*` | Changed |
| `BulletManager` / `EffectManager` | `src/BulletManager.*`, `src/EffectManager.*`, `src/Th07EffectTables.*` | TH06 `BulletManager.*`, `EffectManager.*` | Changed: `BulletManager` now uses the source-derived `FUN_00426f60` single `data/etama.anm` load and the first 10 resolved `DAT_00495160` template script/graze/category rows, while the unresolved eleventh row remains blocked. TH07 effect table, slot layout, spawn paths, draw-list offsets, stage effect ANM loads, first random-splash/render-mode-2 orbit/radial/player-anchored/perspective/camera-snap/eased-radial callbacks, stage-owned camera field mapping, core non-camera Stage position/fog/pause/jump opcodes, Stage ANM VM opcodes `0x1d`/`0x1e`, ANM VM render-helper contracts and source-derived screen/camera/transformed draw helper routing plus `FUN_0044efb0` submit slice, TH07 shake draw offsets plus GameManager frame-counter gate, `FUN_00406930`/`004069d0`/`00406de0` color-multiplier path with common bomb backdrop runtime wiring and `0134cdb0` reset guard, TH07 `FUN_00450d60` instruction header/operand readers, `FUN_00450a50`/`FUN_00450b20` value operand references, `FUN_00450c10`/`FUN_00450ca0` write-target references for simple variable/math/control opcodes through raw `0x51`, Stage ANM VM opcode runtime slice with raw `0x04` jump, raw `0x05` loop, raw `0x0e..0x51` follow-up/interpolation/variable/math/control opcodes, pending-interrupt dispatch, execute-opcode slices through raw `0x51`, and the post-switch VM action contract anchored; remaining callback bodies, original batch-buffer flushing, mode-4 player color producers, broad bullet spawn/update/collision, and laser behavior are pending |
| `ItemManager` | `src/ItemManager.*` | TH06 `ItemManager.*` | Transitional: Cherry/Border behavior differs from TH06; `FUN_00424c00` item-clear radius helper, `FUN_00424740` clear-helper item loop, and shared `FUN_004326f0` visual-effect spawn surface are source-derived as metadata, while full allocation/lifetime, collection, and state-transition runtime replacement is pending |
| `Player` | `src/Player.*`, `src/PlayerLayout.hpp` | TH06 `Player.*` | Changed: Sakuya added; `.sht` behavior and exact runtime replacement pending; ANM VM prefix plus position/collision block are now structurally bridged in `Player.hpp`; `FUN_0043ee50` movement input/direction/focus-speed/clamp and horizontal ANM transition contracts are source-derived; `FUN_0043d160`, `FUN_0043d2f0`, `FUN_0043d690`, `FUN_0043d790`, `FUN_0043d880`, and `FUN_0043d990` now have comparable objdiff-only SHT/raw `0x364` shot-slot spawn/update/draw/timer surfaces; `FUN_0043d0e0` is now `Player::OptionShotEffectCallback` with source-derived `+0x240c` effect cadence and `FUN_0041c1c0` effect-5 spawn behavior; `FUN_0043e0a0`, `FUN_00440940`, `FUN_00441800`, and `FUN_004418b0` now have comparable Player.cpp surfaces for TH07 bomb collision slots; `FUN_004411c0` is now `Player::UpdateMode1Invulnerable` with source-derived 30-frame scale/alpha fade, mode completion, common-effect timer reset, and unresolved `DAT_00575948 + 8` reload; `FUN_00441e80` is now `Player::ResetOptionFields` with source-derived reset offsets, sentinels, thresholds, and pending-state writes; `FUN_0043e4e0` item-box collection uses the TH07 raw mode gate and has an objdiff baseline; `Player::CheckGraze` now uses the source-derived `+0x0960/+0x096c` graze box and `20.0f` margin helper, while kill/graze side effects, live TH07 shot-slot callback dispatch, laser, and mode update/draw branches remain transitional |
| `BombData` | `src/BombData.*`, `src/PlayerBombTables.*`, `src/PlayerLayout.hpp` | TH06 `BombData.*` | Changed: TH07 focused/unfocused slots, player object base/clear span, original player ANM VM prefix layout, position/collision fields, effect durations, common start-effect metadata, mode transition effect metadata, active-effect lifecycle contracts, mode update/draw branch contracts, score-drain factors/minimums, score-drain-step math, backdrop color timing, mode-4 color-frame alias, and speed multipliers anchored |
| `ShtManager` | `src/ShtManager.*`, `src/Th07PlayerShotTables.hpp` | none | Compatibility wrapper over `Th07ResourceManifest`; SHT table data has a single source of truth; `FUN_004423e0` init-time loads now prove the `0049f530/0049f548` path tables and Player `+0xb7e70/+0xb7e74` owner pointer slots |
| `CherrySystem` | `src/CherrySystem.*` | none | Metadata-only: TH07 HUD string anchors plus `FUN_0042f5a2`/`FUN_0042f69f` gain-helper contracts are compiled and PE-checked; `ClearBonusSystem` carries the sourced CherryPoint Max toast draw/register/update/producer contract; broader state transitions pending |
| `BorderSystem` | `src/BorderSystem.*` | none | Metadata-only: TH07 HUD/penalty/rank string anchors are compiled and PE-checked; `ClearBonusSystem` carries the sourced Supernatural Border and Border Bonus toast draw/register/update/producer contracts; state transitions pending |
| `ClearBonusSystem` | `src/ClearBonusSystem.*` | TH06 `Gui.*`/result flow | Source-derived helper: `FUN_00427c81`, `FUN_00427f22`, and `FUN_0042adab` clear-bonus formulas, rank labels, bonus-toast rules/lifecycle/producers, and `+0x209b4..+0x20a2c` owner fields are compiled and PE-checked; full ResultScreen runtime replacement pending |
| `Th07ScoreTables` | `src/Th07ScoreTables.*` | TH06 `GameManager.*` | Source-derived helper: `FUN_0042d8d5` score clamp, GUI-score step, high-score update constants, `FUN_0042d83a` life/bomb extend-award fields/flags, and `FUN_00432990` point-item extend thresholds are compiled and PE-checked; full GameManager owner layout/runtime replacement pending |
| `Th07Constants` | `src/Th07Constants.*` | none | Portable source-derived constants |
| `Th07ResourceManifest` | `src/Th07ResourceManifest.*` | `Th07ResourceManifest.*`, `ShtManager.*` | Portable Ghidra-anchored resource routes, including the authoritative 12-entry SHT path table |
| `ReferencePaths` | `src/ReferencePaths.*` | none | Modern local reference locator |
| `SDL3/Vulkan app` | `src/Th07App.cpp` | none | Modern platform shell |
