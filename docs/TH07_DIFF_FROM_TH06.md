# TH07 Diff From TH06

This document is the working map for source-first TH07 restoration. Every reused TH06 behavior must be checked here before it becomes gameplay authority.

## Status Legend

- `Same`: TH07 is confirmed to match TH06 closely enough for direct reuse.
- `Changed`: TH07 shares the subsystem shape but has different constants, data, state, or edge cases.
- `New`: TH07 adds behavior that TH06 does not have.
- `Pending`: not audited yet.

## Subsystem Map

| Subsystem | Status | Implementation Rule |
| --- | --- | --- |
| Window, fixed timestep, input edge tracking | Changed | Platform layer is modern SDL3, but fixed-frame input semantics must be derived from TH07. |
| RNG and angle helpers | Changed | `FUN_00431870`, `FUN_004318d0`, `FUN_00431900`, and `FUN_00431930` are now source-derived in `src/Rng.*`: the seed transition uses xor `0x9630`, add `0x9aad`, carry mask `0xc000`, shift `14`, and multiplier `4`; `00498b28` is the float `2^32` divisor; `00498a5c/60/64` are `-pi/2pi/pi` angle bounds. `tests/test_rng.cpp` verifies layout, sequence, scaling, and wrap helpers. Replay and caller-level x87 precision/lifetime parity remain pending. |
| Timer/frame stepping | Changed | `FUN_0043958d` is now source-derived in `src/Th07Timer.hpp`: it reads original `DAT_00575ac8` (`Supervisor +0x178`) as the timer multiplier, uses constants `00498a54 = 1.0f` and `00498a58 = 0.99f`, ticks one whole frame for values `> 0.99f`, and accumulates a single subframe carry step for values `<= 0.99f`. `FUN_00439401` and `FUN_004394c7` now source the standalone increment/decrement helper surface, including the `DAT_00575adc` bit-5 force-step branch, `previous = -999`, zero-subframe writes, whole-frame add/subtract paths, subframe carry/borrow loops, and negative-value cross-calls. `FUN_00418fc0` now proves the known bit-5 producer after deriving the timer multiplier, and the producer/read access table is compiled and PE-checked. `Th07EffectTables` routes stage/effect interpolation timers through the tick helper at the current portability boundary. Live `ZunTimer` replacement remains pending until the remaining Supervisor `+0x18c` bits/reset semantics, frame scheduling, and replay/input timing consumers are proven. |
| Archive/resource loading | Changed | TH07 uses `th07.dat`, `thbgm.dat`, and TH07-specific archive/resource manifests. `FUN_00431330` proves the archive lookup path now mirrored in `src/Th07FileSystemTables.hpp`: non-external loads query the single archive owner at `0x00626258`, use `DAT_004b9e64` as the loaded-size global, and log archive misses with string `00497d38`. `FUN_00438668`/`FUN_00438de2` prove that same owner opens `th07.dat`, verifies the generated `th07_0100b.ver` archive probe, and closes via `FUN_0045f800`; `Th07ArchiveReader` parses the original `PBG4` header/index/data stream and validates decoded entries against `reference/extracted/`. `Th07FileSystemRuntime` is now the compiled source-derived portable owner/read helper used by resource smoke checks, but the legacy `FileSystem.cpp` and `Supervisor::LoadPbg3` still carry TH06-style multi-PBG3 storage until startup/menu/stage ownership is replaced. `src/Th07ResourceManifest.*` mirrors the 63 runtime `data/*.anm` string anchors from `reference/ghidra/strings.tsv` and the 46 source-derived load slot/offset contracts currently proven by `FUN_00401d70`, `FUN_00407000`, `FUN_0041e590`, `FUN_00426f60`, `FUN_00428b19`, `FUN_00438986`, `FUN_0043ae20`, `FUN_004423e0`, `FUN_00449b05`, and `FUN_0045bf15`; `scripts/audit-th07-resources.mjs` now scans live `LoadAnm` calls and keeps the 22 paths with no TH07 string anchor marked as obsolete TH06 carryover rather than source-derived resources. |
| ANM parsing | Changed | thcrap marks TH07 as `anm06`; the raw file format is same-family while the manager contract is TH07-specific. `scripts/audit-th07-resources.mjs` and the compiled `src/Th07AnmFile.*` parser now check 63 extracted ANM files through the raw header/sprite/script tables, covering 1187 sprite records, 727 script records, 6214 non-terminal ANM VM instructions, 727 `-1` terminators, opcode coverage including max opcode `80`, 44 aggregate opcode usage rows, 44 opcode argument-shape rows totaling 38724 payload bytes, and instruction sizes `8,12,16,20,24,28` before deeper opcode parity work. `tests/test_anm_parser.cpp` validates aggregate counts and spot-checks `etama.anm`, `player02.anm`, and `capture.anm` against compiled metadata. `src/Th07AnmManagerLoadTables.hpp` now adds the original `FUN_0044df90`/`FUN_0044e070`/`FUN_0044e4e0` manager load/free contract: 0x32 slot cap, version-2 header requirement, owned-file bit, chained-entry loaded count, 0xa00 sprite/script global id cap, and manager slot/script/texture table offsets. `tests/test_anm_manager_load_contract.cpp` PE-checks that contract and validates the 46 runtime load rows against extracted ANM data. Live `AnmManager::LoadAnm` remains transitional until texture/backend lifetime and dispatch are proven. |
| STD parsing | Changed | TH07 has `stage1-8.std`; `scripts/audit-th07-resources.mjs` and the compiled `src/Th07StdFile.*` parser now parse all eight STD binaries through the `RawStageHeader`/object/quad/instance/script layout used by `FUN_00407610`, covering 45 objects, 162 quads, 906 instances, 378 script instructions, 25 opcode usage rows across script opcodes `0..31` with gaps, and 25 fixed-size argument-shape rows totaling 4536 argument bytes. `src/Th07StageLoaderTables.hpp` now records the matching loader contract from `FUN_00407610`: `FUN_00431330` file load, owner fields `+0x49c/+0x4a0/+0x4a4/+0x4a8/+0x4ac/+0x4b0`, object-offset table relocation from file `+0x490`, quad VM allocation as `declaredQuadCount * 0x24c`, object flag byte `+3 = 1`, and quad setup through `FUN_00404f30(vm + index * 0x24c, anmScript + 0x300)`. `src/Th07ResourceFormatTables.*` carries the per-stage binary summaries, aggregate opcode usage, and fixed `0x0c` argument-block coverage, while `tests/test_std_parser.cpp` and `tests/test_stage_loader_contract.cpp` validate the portable parser, original loader bytes, and raw object/quad sentinel walks, including Stage 6's 2 objects, 4 quads, 36 instances, 58 instructions, offsets `0x548/0x798`, and opcode mask. The readable dump coverage is still 7/8 because `reference/std/stage6.dstd` is empty, so Stage 6 work should rely on `stage6.std` binary evidence until that dump is refreshed. |
| MSG parsing | Changed | thcrap marks TH07 as `msg06`; the file format still follows the msg06 family, but the runtime surface is not the copied TH06 subset. The resource audit and compiled `src/Th07MsgFile.*` parser now verify all eight readable MSG dumps with 60 entries and 1098 timeline labels plus all eight original `msg*.dat` binaries with 180 entry pointers, 60 unique entry scripts, 4883 instructions, opcode coverage `0..14`, 15 aggregate opcode usage rows, and 15 aggregate argument-size rows totaling 46572 argument bytes. `tests/test_msg_parser.cpp` cross-checks pointer-table aliases, per-stage summaries, opcode masks, text-byte totals, wait-frame totals, and Stage 6 opcode `0x0e` usage against `src/Th07ResourceFormatTables.*`. `src/Th07GuiMsgLoaderTables.hpp` now records the matching `FUN_00429935`/`FUN_004299f8` loader/free contract: `FUN_00431330` non-external file load, owner fields `+0x1fba4/+0x1fba8/+0x1fbac`, failure logging through `FUN_004315f0`, raw entry-pointer relocation from file `+4`, and `_free` cleanup. `tests/test_gui_msg_loader_contract.cpp` PE-checks the call sites and validates raw pointer-table relocation across all eight MSG binaries. `FUN_00429c42` now proves the full 15-entry MSG switch table at `0x0042a83a`, case targets from `0x00429d42` through `0x0042a71b`, the common instruction advance at `0x0042a730`, and the post-tick ANM VM path at `0x0042a766`. Opcodes `0x01/0x02` now use TH07 portrait script/sprite bases, including the enemy sprite offset `0x0d` instead of copied TH06 `+8`; opcodes `0x0c..0x0e` also have source-derived runtime actions: music fadeout, wait-skippable byte write, and full-screen white fade. The copied enum/runtime no longer stops at TH06's `0..13` surface. Remaining MSG opcode behavior still needs function-level confirmation before the VM can be called exact. |
| ECL instruction core | Changed | TH07 `ecldata1-8.ecl` manifest is anchored, and `scripts/audit-ecl-opcodes.mjs` now audits all eight extracted `.decl` dumps: 141 unique script opcodes appear, with max opcode `161`. The current copied enum has 136 opcodes, the current dispatch handles 134, and the copied enum/dispatch is missing 25 TH07-used high opcodes (`136..146`, `148..161`). The original `00410520` dispatch subtracts one from the raw opcode and jumps through the table at `0x00417838`, so `config/ecl_opcode_dispatch.csv` now tracks jump-table entry/target addresses alongside decompiler case labels. `src/Th07EclFile.*` now compiles the ECL binary parser and validates all eight `reference/extracted/ecldata*.ecl` binaries through the `0x40` header/table, sub instruction shape, and timeline instruction shape: aggregate coverage is 762 subs, 17 timelines, 19919 sub instructions, 2494 timeline instructions, and 141 unique sub opcodes, with `.decl` `ins_` counts and high-opcode counts cross-checked by `tests/test_ecl_parser.cpp`. `src/Th07EclManagerTables.hpp` records the original `FUN_0040e420` manager loader and `FUN_0040e530` call-sub contract: the loader relocates 16 header entries through file offset `0x40`, sets the runtime sub-offset table to `file + 0x44`, skips the file sentinel entry at `0x40`, then relocates `subCount` entries; call-sub writes the current instruction, timer sentinels/zeros, and sub id at context `+0x214`. `src/EclOpcodeTables.*` mirrors the enum/dispatch gap arrays plus those 25 high-opcode records, 31 observed high-opcode operand signatures, 41 direct owner write contracts, 17 helper-call/resolved-target call contracts, 70 `FUN_0040e5b0` int-read resolver contracts, 74 `FUN_0040edf0` float-read resolver contracts, 37 `FUN_0040f3c0` resolved float write-target contracts, the `FUN_00424740` clear-helper surface, the `FUN_00424c00` item-clear radius helper, the `FUN_004326f0` 0x44c-slot visual-effect spawn contract, opcode `151` x87 projection wrappers/cores (`0048bb40`/`0048bbf0` through `0048bb5d`/`0048bc0d`), and the `FUN_00455170` opcode `138` record-builder helper into compiled metadata; `tests/test_ecl_opcode_tables.cpp` verifies the all-source-derived split, signature variants such as `ins_140` float references and void `ins_146`, the opcode `141` common-advance target, selected enemy/global/pointer-table read/write offsets, RNG/vector/distance helper surfaces, x87 projection helper metadata, and PE-checked call targets/resolver/helper cases. Runtime replacement must still prove owner fields, lifetimes, remaining header-offset meanings, TH07 context layout, and remaining helper callees before wiring. |
| Enemy and boss lifecycle | Changed | Extra/Phantasm, boss UI, and spell handling require TH07 function replacement. |
| Bullet/effect manager | Changed | TH07 enemy bullet sheet evidence currently proves a single `data/etama.anm` resource at string address `00498524`, parsed from `reference/extracted/etama.anm` as 168 sprites and 25 scripts. `FUN_00426f60` and `DAT_00495160` now prove the original 11-row enemy bullet template script table, and `src/Th07BulletTables.hpp` resolves slots `0..9` against `etama.anm` active sprites plus the original graze/category constants; `src/BulletManager.cpp` now uses that source-derived single load and those 10 resolved live template rows. Slot `10` is deliberately blocked because `0x2a8/0x2aa/0x2a9` are not available under the proven single `etama.anm + 0x200` load. The copied TH06 split names `etama3/etama4` and `ANM_FILE_BULLET3/BULLET4` remain obsolete carryover records/aliases only. TH07 bullet spawn/update/collision runtime and the 34-entry TH07 effect table/layout must replace TH06 assumptions; effect slots, spawn/update manager layout, stage effect ANM loads, bomb common effect id `0x19`, first TH07 random-splash/render-mode-2 orbit/radial/player-anchored/perspective/camera-snap/eased-radial callbacks, the stage camera opcode/interpolation model, core non-camera Stage position/fog/pause/jump opcodes, stage ANM VM opcodes `0x1d`/`0x1e`, ANM VM render-helper contracts, the TH07 `FUN_00450d60` 16-bit instruction header/operand readers, `FUN_00450a50`/`FUN_00450b20` value operand-reference variable slots, `FUN_00450c10`/`FUN_00450ca0` write-target resolver behavior for the simple variable/math opcode slice, execute-opcode slices through raw opcode `0x51`, and the `FUN_00450d60` post-switch VM action contract are anchored. `Stage` now exposes the TH07 `0x52b4` camera block, dispatches the camera opcode subset and core non-camera stage opcodes, and executes the source-derived stage ANM VM script slots through a TH07-header-driven opcode slice with raw `0x04` jump, raw `0x05` loop, value operand references, raw `0x0e..0x51` follow-up/interpolation/variable/math/control opcodes, source-derived screen-quad draw helper routing, and pending-interrupt label dispatch; `EffectManager` routes draw lists through the source-derived TH07 screen-quad, camera-projected, and transformed camera-projected helpers. The copied TH06 20-entry runtime skeleton, the Stage ANM VM call to TH06 `AnmManager::ExecuteScript`, the TH06 `DrawInner` bridge for these TH07 draw routes, and the TH06 rectangle-darken bomb backdrop have been removed. TH07 screen shake now feeds ANM manager draw offsets and uses the mapped GameManager frame-counter gate, Stage has the source-derived color-multiplier write/consume/reset path, the `004083f0` reset-guard write at `0134cdb0` is mapped to stage offset `+0x52b0`, and the player/bomb producers plus active-effect lifecycle and mode update/draw branch contracts that feed that path are mapped in `PlayerBombTables`. Remaining renderer gaps are original batch-buffer flushing and runtime replacement for the mode-4 `FUN_00406930` producers. |
| Laser behavior | Pending | Audit TH07 ECL and executable before reuse. |
| Item manager | Changed | Cherry, Cherry+, Border Bonus, and TH07 point item behavior are authoritative. `FUN_00424c00` proves the ECL opcode `143` radius-clear helper over `DAT_0063b218`: 0x400 item slots, 0x0d68-byte stride, state at `+0xbfc`, position at `+0xb8c`, skip state `5`, effect spawn `FUN_004326f0(pos, 6, 1)`, and 0x35a-dword slot clear. `FUN_00424740` proves another ECL clear helper over the same item array plus a separate owner-relative 0x40-entry object array at `+0x366628`; `FUN_004326f0` is now mapped as the shared visual-effect spawn surface with 0x44c slots, 0x288-byte stride, fallback slot `+0xae060`, and next index `+0xae2e8`. Full item allocation/lifetime and collection state transitions remain pending. |
| GameManager score display and extend award | Changed | `FUN_0042d8d5` proves TH07's GUI-score step core: score clamps to `999999999`, the GUI delta is shifted by `5`, the step is capped at `0x8d55e`, and minimum nonzero step is `1`. The same slice proves high-score replacement writes score field `+0x0c` and copies current shot byte `+0x20` into high-score shot byte `+0x10`. `FUN_0042d83a` proves the adjacent extend-award helper: it awards owner life stock `+0x5c` below `8`, otherwise bomb stock `+0x68` below `8`, plays sound `0x1c`, starts a 200-frame notification, and writes `DAT_0049fbf4` life/bomb notification bits. `FUN_00432990` proves TH07's point-item life-extend threshold formula and calls into `FUN_0042d83a`; this replaces the old assumption that the copied static TH06 score-threshold array was authoritative. `src/Th07ScoreTables.*` carries this as source-derived helpers; the copied runtime still must not be treated as authority because it keeps TH06-style `78910`, modulo-10 stepping, and an unevidenced copied static score-extend array. |
| GUI spell-card display | Changed | `FUN_0042868d` proves TH07 spell-card display owner offsets and constants: owner pointer `+0x08`, text width `+0x0c`, VM slots `+0x574c/+0x5be4/+0x607c/+0x6514`, interrupt VM `+0x69ac`, script ids `0x04a1/0x04a4/0x04a6/0x0704`, default name sprite `0x04ac`, text color `0x00f0f0ff`, sound `0x0e`, and global state `00575ab4 = 2`. `FUN_00429935`/`FUN_004299f8` prove the MSG load/free owner contract and pointer-table relocation used before dispatch. `FUN_00429c42` additionally proves the 15-entry MSG dispatch table, the common advance/post-tick paths, opcode `0x01` portrait script base/enemy offset, opcode `0x02` portrait sprite base/enemy offset, opcode `0x0c` as the 4.0-second music fadeout call, opcode `0x0d` as the one-byte wait-skippable flag write, and opcode `0x0e` as the stage-6 full-screen white fade trigger. `src/GuiLayout.hpp` and `src/Th07GuiMsgLoaderTables.hpp` carry the evidence constants and `Gui.cpp` now has the source-derived opcode cases; the broader copied `GuiImpl` runtime remains transitional until allocation/lifetime/dispatch and the remaining opcodes are mapped. |
| Player roster | New | TH07 has Reimu, Marisa, and Sakuya, each with A/B shot types. The live `g_CharData` table now has all six entries and pulls movement speeds from the source-derived SHT summary table (`Reimu 4.0/1.6`, `Marisa 5.0/2.0`, `Sakuya 4.0/2.2`). Sakuya shot callbacks are pending placeholders that stop spawning rather than reuse another character's behavior. |
| Player shot data | New | 12 `.sht` files, 582 shot records, power thresholds, callback usage counts, high-level SHT callback semantics, original `0049f530/0049f548` unfocused/focused path tables, and Player init-time owner pointer loads into `+0xb7e70/+0xb7e74` are Ghidra/binary anchored and mirrored in `src/Th07PlayerShotTables.hpp` plus `src/PlayerLayout.hpp`; `Player::SpawnBullets`, `Player::UpdatePlayerBullets`, `Player::DrawBullets`, `Player::DrawBulletExplosions`, `Player::StartFireBulletTimer`, and `Player::UpdateFireBulletsTimer` now have comparable objdiff-only SHT/raw `0x364` shot-slot spawn/update/draw/timer surfaces, but live behavior replacement still waits on replacing the transitional `PlayerBullet` layout. |
| Bomb behavior | Changed | Focused/unfocused bomb routine slots, player object base/clear span, original `0x24c` player ANM VM prefix layout, player position/movement/hitbox/graze/grab-item field offsets, raw `FUN_0043ee50` movement input/direction/focus-speed/clamp contract, raw mode state `+0x2408`, option/focus state `+0x240a/+0x240b`, option-shot effect cadence byte `+0x240c`, transition request `+0x240d`, common-effect durations, common start-effect metadata, score-drain factors/minimums, score-drain-step math, common backdrop color timing/runtime color-write wiring, mode-4 stage-color producers, mode transition effect metadata, active-effect lifecycle contracts, mode update/draw branch contracts for `FUN_00441330` and `FUN_004420b0`, the `DAT_004d44e0 == DAT_004bdad8 + 0x16a08` alias, and movement speed multipliers are binary/decompiler anchored. `Player.hpp` now structurally carries the leading TH07 VM/position block, `+0x17dc` bomb collision slots, `+0x23f0/+0x23f4` speed fields, `+0x16a00..+0x16a14` common-effect tail, and active pointers at `+0xb7e68/+0xb7e6c`; `Player.cpp` seeds the initial low-byte mode/option/request state plus `FUN_004423e0` common-effect defaults, exposes comparable source surfaces for `FUN_0043d0e0`, `FUN_0043e0a0`, `FUN_00440940`, `FUN_004411c0`, `FUN_00441800`, `FUN_004418b0`, and `FUN_00441e80`, and now routes graze geometry through the TH07 `+0x0960/+0x096c` box with the original `20.0f` margin. `FUN_0043d0e0` records the SHT collision callback that spawns effect id `5` every 8 hits via `FUN_0041c1c0`; `FUN_004411c0` records the mode-1 invulnerability scale/alpha fade and completion reset through `DAT_00575948 + 8`; `FUN_00441e80` resets raw option fields at `+0x2428..+0x2440` and records the pending-state threshold writes through `013542ec/0134d476`; global and shot-slot ownership remains pending for these surfaces. The live bomb table has six slots; Sakuya slots are explicit null placeholders until their original routines are reconstructed. Runtime producer dispatch and owner mutation wiring remain pending. |
| Point of Collection | Changed | Use original TH07 full-power/border rules; no Web modernization. |
| Hitbox display | Changed | Debug-only overlay may exist; release gameplay must match original visibility. |
| Cherry / Cherry+ | New | HUD string anchors are compiled and PE-checked, including `CherryPoint Max!` and `Cherry = %8d`; `FUN_00427f22` proves the CherryPoint Max toast draw contract for kind `3`, `FUN_00427c81`/`FUN_0042adab` prove the shared toast registration/update lifecycle, and `FUN_0042f5a2`/`FUN_0042f69f` prove the two CherryPoint Max producer calls. `CherrySystem` now also carries source-derived gain-helper contracts: both helpers increment/clamp manager `+0x961c` against `+0x9618`, while the primary `FUN_0042f5a2` path additionally updates `+0x9620`, gates on `DAT_004bfee5`, clamps against `*(owner+8)+0x88 + 50000`, and calls `FUN_00441960`. Remaining start/end/scoring state transitions still require decompilation. |
| Supernatural Border | New | HUD/penalty/rank anchors are compiled and PE-checked, including `Border Bonus %7d`, `Supernatural Border!!`, player-penalty labels, and `Phantasm Rank*2.0`; `FUN_00427f22` proves the Supernatural Border and Border Bonus toast draw contracts for kinds `2` and `4`, `FUN_00427c81`/`FUN_0042adab` prove the shared toast registration/update lifecycle, and `FUN_00441670`/`FUN_00441960` prove the Border Bonus and Supernatural Border producer calls. Remaining start/break/end transitions still require decompilation. |
| Clear/result bonus | Changed | `FUN_00427c81`, `FUN_00427f22`, and `FUN_0042adab` prove TH07 clear-bonus formulas, rank labels, notification strings, four bonus-toast kind rules, the toast timer lifecycle, and the Result/GUI owner fields from `+0x209b4` through `+0x20a2c`; the mapped producer table adds seven calls from Cherry, item collection, Border Bonus, and Supernatural Border paths. `src/ClearBonusSystem.*` now carries these as compiled source-derived helpers without depending on `GameManager.hpp`; full ResultScreen owner lifetime, broader state transitions, dispatch, score-file writes, and spell-card result structures still need mapping before runtime wiring. |
| Spell card score/result | Changed | TH07 has 141 spell ids; score/result structures still need mapping. |
| Stage list | Changed | Main stages 1-6 plus Extra stage 7 and Phantasm stage 8. |
| BGM streaming/loop | Changed | Use `thbgm.fmt` + `thbgm.dat`; modern audio backend must preserve original loop metadata. |
| SFX table | Changed | TH07 adds `se_bonus`, `se_bonus2`, `se_border`, and `se_pause`. |
| C++ source reference | New | `src/` is the TH06-derived baseline being replaced. |

## Immediate Audit Queue

1. Map TH07 ANM manager draw-offset/global-color producers and decide the exact boundary for original batch-buffer flushing versus the portable renderer.
2. Resolve the blocked enemy bullet template slot `10` (`0x2a8/0x2aa/0x2a9`) and then map bullet spawn/update/collision runtime contracts before replacing the copied TH06 bullet manager split-bank runtime.
3. Use the compiled ECL manager loader/call-sub and helper-call/resolved-target high ECL contracts only after owners/lifetimes, remaining header-offset meanings, TH07 context layout, and helper callees are proven; `FUN_0040e420`/`FUN_0040e530`, `FUN_0040e5b0`/`FUN_0040edf0` read resolvers, `FUN_0040f3c0` write-targets, `FUN_00424740`, the `FUN_00424c00` item-clear helper, the `FUN_004326f0` visual-effect spawn surface, and the `FUN_00455170` opcode `138` record-builder helper are mapped, but opcode `141` remains a common-advance no-op until runtime naming is proven.
4. Use the compiled ANM parser plus `FUN_0044df90`/`FUN_0044e070`/`FUN_0044e4e0` manager load/free contract as the AnmManager loader baseline only after texture/backend lifetime and dispatch are proven; use the compiled STD parser plus `FUN_00407610` Stage loader contract as the next Stage loader baseline only after draw-object VM ownership/runtime behavior is proven; use the compiled MSG parser as the next Gui MSG loader baseline only after owner lifetimes are proven; refresh the Stage 6 readable dump only when needed.
5. Use the expanded Player owner tail and active-effect lifecycle contracts to prove mode/common-effect producer dispatch order before replacing player-shot callbacks and full bomb runtime.
6. Map the `FUN_00432990` owner allocation/lifetime before replacing copied score/extend runtime; keep the static TH06 score-threshold array excluded unless a TH07 source is found.
7. Source Cherry, Cherry+, and Border state transitions from Ghidra before gameplay implementation.
